# MyBot 업무지침서 — 그록봇 동급화 로드맵

> 작성: 2026-09-16 · 기준 커밋 `5b06850` · 대상: MyBot (Bun + Hono + SQLite + React + Playwright)
> 짝 문서: `업무절차서.md` = **지나온 기록(Phase 0~15)**, 이 문서 = **앞으로 할 일(Phase 16~23)**
> 근거: 소스 전수 판독 + 운영 DB 실측(`agent_runs` 629건 / `approval_requests` 240건 / `tool_log` 3,465건 호출) + 그록봇 공식 문서(docs.x.ai) 대조

---

## 0. 이 문서 사용법

1. 이 문서는 **단계(Phase)별 작업 지시서**다. §6의 체크박스를 위에서부터 처리한다.
2. 한 Phase가 끝나면 **반드시 §7의 갱신 규칙대로 이 문서를 수정한다** — 상태표 갱신, 진행 로그 1줄 추가, 새로 발견된 항목을 §3~§5에 추가.
3. `업무절차서.md`는 Phase 15까지에서 멈춰 있고 이후 커밋(승인 경계·PGE 루프·LLM 의도분류·명령 대기열·실행 분리)이 반영돼 있지 않다. **Phase 16에서 절차서부터 현행화**한다.
4. 판단 원칙 — 있으니까 넣지 말고, **실측 실패를 줄이는 것부터** 넣는다. 넣어서 문제가 생기면 미련 없이 걷어낸다.

---

## 1. 기준선 — 그록봇의 실제 기능 대비 현황

그록봇(Grok Bot, SpaceXAI, 2026-08 베타)의 공식 문서 기준 기능과 MyBot 현재 구현의 1:1 대조다.
판정: **○ 동등** / **△ 부분** / **✕ 없음**

| # | 그록봇 기능 | MyBot 현재 구현 | 판정 |
|---|---|---|---|
| 1 | 봇별 지속 역할(이름·직함·설명·아바타), 지속규칙 vs 일회지시 구분 | `agents` 테이블, `role_prompt`, `face:` 아바타. 지속/일회 구분 없음 | △ |
| 2 | 상시 실행 — 사용자 기기 꺼도 클라우드에서 계속 | launchd 상주 + 실행/HTTP 분리(`activeRuns`). 맥미니가 꺼지면 중단 | ○ |
| 3 | 봇 전용 컴퓨터: **브라우저 + 파일시스템 + 터미널** | 브라우저(Playwright) ○ / 파일(WORK_DIR 샌드박스) ○ / **터미널 ✕** | △ |
| 4 | 컴퓨터 뷰 — 봇 화면 실시간 관찰 | 없음. 텍스트 도구 칩만 표시 | ✕ |
| 5 | **테이크오버** — 비밀번호/2FA/CAPTCHA 시 사람이 제어 인계 후 반환 | 없음. `request_credentials` 팝업(계정 저장)만. 2FA·CAPTCHA 만나면 실패 | ✕ |
| 6 | 브라우저 UI 조작(클릭·입력·탐색) | `@번호` 참조 스냅샷 + iframe 재귀 + 수렴 대기 + 팝업 추적 + `browser_wait/back` (Phase 17) | ○ (실사용 검증 대기) |
| 7 | 스킬 — 글로 저장 | `skill_save` / `skill_list`, 전 봇 공유, `/` 슬래시 호출 | ○ |
| 8 | 스킬 — **시연 녹화(Teach a task, 최대 10분) → 초안 자동 생성** | 없음 | ✕ |
| 9 | 루틴 — 스케줄 트리거 | `every:Nm/Nh`, `daily:HH:MM` | ○ |
| 10 | 루틴 — **이벤트 트리거(Slack·GitHub 등)** | 이메일(IMAP) 트리거만 | △ |
| 11 | 루틴 운영 UI — 활성/일시정지·테스트실행·편집·성공실패 이력 | 활성/토글·지금실행·삭제 ○ / **실행 이력 조회 ✕** | △ |
| 12 | 루틴 한도 — 봇당 50개, 실행이력 최근 20건 보관 | 한도 없음, 보존 정책 없음 | ✕ |
| 13 | **Auto Review 승인 규칙** — "Ask first" / "Allow automatically", ask first 우선 | `approval_rules` require/allow, require 우선. 정책 동일 | ○ |
| 14 | 승인 대상 = 액션·범위 단위의 좁은 규칙 | **도구명 정규식만** — "외부 메일 발송 전 승인" 같은 인자 조건 불가 | △ |
| 15 | 봇 간 DM(비동기 핸드오프) | `agent_message` + `dispatchAgentMessage` | ○ |
| 16 | 그룹 채팅(여러 봇 한 대화, 핸드오프 가시화) | `groups` + 그룹 대화 루프, `@멘션` 지정 | ○ |
| 17 | 프로젝트 — 오케스트레이터 + 전문봇의 공유 컨텍스트 공간 | `workspaces`(지침 주입만). 봇 배정·공유 메모리 없음 | △ |
| 18 | 메모리 3계층 — 사용자 / 봇 / 프로젝트 | 사용자(`agent_id IS NULL`) + 봇(`agent_id`) 2계층. 프로젝트 계층 ✕ | △ |
| 19 | 봇 복제 — 프로필·설정·스킬·루틴 복사, 대화·메모리 제외 | `POST /agents/:id/duplicate` (스킬 복사 ○, **루틴 복사 ✕**) | △ |
| 20 | 봇 공유 — 퍼블릭 링크로 구성만 배포 | 없음 | ✕ |
| 21 | 커넥터/플러그인 + **MCP (remote HTTP)** | MCP **stdio만** 지원. remote HTTP ✕ | △ |
| 22 | 모바일 컴패니언 | PWA + LAN/HTTPS 접속 | ○ |
| 23 | 모델 선택 | **MyBot이 우위** — 9개 프로바이더 직결 + 커스텀 + CLI 어댑터 | ◎ |
| 24 | 로컬 파일 접근 | **MyBot이 우위** — 셀프호스팅이라 로컬 파일 접근 가능 | ◎ |
| 25 | 데이터 소유 | **MyBot이 우위** — 전부 로컬 SQLite | ◎ |
| 26 | 봇 수 상한(봇+그룹챗 50개) | 상한 없음 → §2-③의 폭주 원인 | ✕ |
| 27 | 감사 로그 | `agent_runs`에 적재되나 **조회·필터 UI 없음** | △ |

**결론**: 그록봇 대비 **모델 자유도·데이터 소유·로컬 접근은 이미 우위**다. 진짜 격차는 딱 네 곳 —
**(A) 브라우저 조작의 신뢰성, (B) 사람 인계(테이크오버)와 화면 관찰, (C) 시연 기반 스킬 학습, (D) 조직 거버넌스와 실행 안정성.**

---

## 2. 실측 진단 — 숫자로 드러난 문제

운영 DB(`server/data/mybot.db`)를 직접 집계한 결과다. 추측이 아니라 실제 기록이다.

### ① 브라우저 조작이 절반만 성공한다 — 최우선 결함

| 도구 | 호출 | 실패 | 실패율 | 평균 소요 |
|---|---:|---:|---:|---:|
| `browser_eval` | 112 | 0 | 0% | 0.2초 |
| `browser_open` | 57 | 0 | 0% | 2.0초 |
| `browser_click` | **24** | **12** | **50%** | 4.7초 |
| `browser_read` | 21 | 0 | 0% | 0.1초 |
| `browser_look` | **10** | **7** | **70%** | **97초** |
| `browser_type` | **2** | 0 | — | 0.9초 |

읽어야 할 것은 **실패율이 아니라 호출 분포**다.
- `browser_click` 실패 12건 전부 `Timeout 8000ms exceeded — waiting for locator('text=받은메일함')` 류다. **모델이 셀렉터를 추측했고, 틀렸다.**
- 그래서 모델은 셀렉터 도구를 포기하고 **`browser_eval`(생 JS)로 우회**했다 — 112회. `browser_type`은 단 2회.
- 즉 **설계한 도구 세트가 실사용에서 버려졌다.** 원인은 `snapshot()`(browser.ts:129)이 `innerText` + 링크 30개만 돌려주기 때문이다. 모델은 클릭 가능한 요소가 뭔지, 그 요소를 어떻게 지목해야 하는지 알 수 없다.
- `browser_look`은 매 호출마다 풀페이지 PNG를 `defaultModelId()`(비전 모델이 아닐 수 있음)에 태워 **7/10이 120초 타임아웃**으로 죽었다.

### ② 실패한 실행의 절반이 rate limit이다

`agent_runs` 629건 중 실패 125건(19.9%). 사유 분포:

| 사유 | 건수 | 비중 |
|---|---:|---:|
| `429 rate_limit_error` (minimax 등) | **65** | 52% |
| 서버 재시작으로 중단 | 34 | 27% |
| chatOnce 실패 / 타임아웃 / CLI 타임아웃 | 22 | 18% |
| 잔액 부족(1113) | 3 | 2% |

- 429는 `openaiCompat.ts:63`에서 **1회만 재시도**하고 포기한다. **다른 프로바이더로의 폴백이 없다.** 9개 프로바이더를 등록해 놓고도 한 곳이 막히면 작업이 죽는다.
- 재시작 중단 34건은 `index.ts`에서 `running → error`로 마킹만 하고 **재개하지 않는다**. 8분짜리 작업이 통째로 증발한다.

### ③ 봇 조직이 자기 자신을 재편하느라 돌고 있다

봇은 **8개**뿐인데 도구 호출 기록은 이렇다:

| 도구 | 호출 수 |
|---|---:|
| `agent_list` | **730** |
| `agent_delete` | **594** |
| `agent_direct` | 414 (평균 71.7초, 최대 **567초**) |
| `agent_create` | 155 |
| `agent_update` | 108 |

승인 팝업 240건의 내역:

| 도구 | 승인 | 거부 |
|---|---:|---:|
| `agent_delete` | 76 | **60** |
| `agent_create` | 57 | **26** |
| `routine_delete` | 19 | 1 |

- **조직 관리 도구(`agent_list`+`agent_delete`+`agent_create`+`agent_update` = 1,587회)가 전체 도구 호출 3,465회의 46%를 차지한다.** 실제 업무 도구(`web_search` 80, `browser_open` 57)보다 훨씬 많다. 봇들이 "일"이 아니라 "조직 개편"을 하고 있다.
- 승인 240건 중 **87건(36%)을 사용자가 거부**했다. 승인 게이트가 폭주를 막긴 했지만, 그 대가로 **사람이 팝업 처리기가 됐다.** 이건 게이트의 승리가 아니라 상위 설계의 실패 신호다.
- `agent_direct` 최대 567초. 상위 실행의 데드라인은 8분(480초)이다 — **하위 위임이 상위 예산을 초과**해 상위가 먼저 죽는 구조다.

### ④ 작업 디렉터리와 메모리가 관리되지 않는다

- `server/data/workspace/` 루트에 **임시·초안 파일 17개**(`_` 접두 14 + draft/example 3) 방치: `_ip_bot_test.ts`, `_tmp_mail_test.ts`, `_mail_stream_out.txt`, `_modal_inspect.ts`, `_read_approval_detail.py` …
  → 봇들이 **`.ts`/`.py` 스크립트를 쓰고 있다. 그런데 MyBot에는 그걸 실행할 도구가 없다**(터미널 부재). 실행할 수 없는 코드를 계속 생산 중이다 = §1-③ 터미널 부재의 직접 증거.
- `workspace/agents/` 폴더 **12개** vs DB 봇 **8개** → 고아 폴더 5개(`AI 뉴스 브리핑봇`, `IP 신청봇`, `결제 처리봇`, `메일 브리핑봇`, `논문분석팀장`), 반대로 `비서실장봇`·`개발봇`은 폴더 없음.
- **이름 중복 사고**: `IP 신청봇` / `IP신청봇`, `결재처리봇` / `결제 처리봇`(오타) — 이름 변경 시 `renameSync`가 조용히 실패(`catch {}`)해 **봇이 자기 MEMORY.md를 잃었다.**
- **자모 분리(NFD) 중복**: `논문분석_파이프라인_조직도.md` / `논ᆫ문분석_파이프라인_조직도.md` — macOS 파일명 정규화 미처리.
- `memories` **636건**, `messages` 1,497건, `agent_runs` 629건, `approval_requests` 240건 — **보존·만료 정책 전무**. `recallMemories`는 LIKE 스캔이라 증가할수록 회상 품질이 떨어진다.
- `server/data/browser-profile/` **326MB** — 캐시 정리 루틴 없음.

### ⑤ 스키마와 실행 루프가 두 곳에 흩어져 있다

- `skills` 테이블이 `db.ts`가 아니라 `routes/workspaces.ts:29`에서 생성된다. `db.ts:…`의 `ALTER TABLE skills ADD COLUMN agent_id`는 **신규 설치에서 조용히 실패**하고, workspaces.ts가 뒤늦게 다시 ALTER한다. 스키마 단일 소스 원칙 위반.
- 도구 루프가 **두 벌**이다: `routes/chat.ts`(4라운드) / `team.ts`(12라운드). `parseLeaked`는 양쪽에 **복붙 중복**. 동작이 달라 "대화에서 되는 게 위임에선 안 되는" 버그의 온상.
- 승인 후 재개 경로가 **세 곳**: `approvals.ts:resumeAgent`, `browser.ts` 계정입력 재개, `approvals.ts:dispatchAgentMessage`. 셋 다 같은 코드를 다르게 반복한다.
- `chat.ts:640~740`의 자가교정 블록 — **정규식 조건 10개가 인라인 100줄**로 얽혀 있다. 조건 하나 고치면 다른 조건이 깨진다.

---

## 3. 제거해야 할 것 (Remove)

| ID | 무엇을 | 왜 (근거) | 어떻게 | 완료 기준 |
|---|---|---|---|---|
| **R1** | `workspace/` 루트의 임시 파일 17개 (`_`로 시작하는 전부 + `*_test*`, `*draft*`) | 봇의 `list_files`/`read_file` 결과를 오염시켜 **매 실행마다 컨텍스트를 낭비**하고, 봇이 과거 실험 파일을 현재 데이터로 오독한다 | `workspace/_archive/2026-09/`로 이동 후 정리. 이후 `_` 접두 파일은 `write_file`에서 `workspace/tmp/`로 강제 라우팅 | `list_files()` 루트 출력이 20줄 이내 |
| **R2** | 고아 봇 폴더 5개 + 이름 중복 폴더(`IP 신청봇`, `결제 처리봇`) | DB에 없는 봇의 MEMORY.md가 남아 **다른 봇이 읽고 남의 기억을 자기 것으로 보고**한다. 중복은 이름변경 실패 흔적 | DB `agents.name`과 대조해 고아는 `_archive/`로, 중복은 정본에 병합 | `ls workspace/agents` == DB 봇 목록 |
| **R3** | NFD 자모 분리 중복 파일 (`논ᆫ문분석…`) | macOS NFD/NFC 불일치로 **같은 파일이 두 개로 보이고**, 봇이 둘 다 읽어 중복 보고 | 파일명 NFC 정규화 후 병합. `safePath()`에 `String.prototype.normalize("NFC")` 추가 | 중복 파일명 0건 |
| **R4** | `team.ts`의 `parseLeaked` 중복 정의 (chat.ts에도 동일 함수) | 한쪽만 고치면 **다른 경로에서 도구 누수 응답이 그대로 통과**한다 | `server/src/toolloop.ts`로 추출해 양쪽이 import | 동일 함수 정의 1개 |
| **R5** | `routes/workspaces.ts`의 `CREATE TABLE skills` + 뒤따르는 ALTER | 스키마가 두 파일에 분산돼 **신규 설치와 기존 설치의 스키마가 갈라진다**(§2-⑤) | `db.ts`로 이관, `workspaces.ts`에는 시드(INSERT OR IGNORE)만 남김 | `grep -c "CREATE TABLE" server/src/routes/` == 0 |
| **R6** | `browser_look`의 현행 구현 (풀페이지 스크린샷 → 기본 모델) | 호출 10건 중 **7건이 120초 타임아웃**(§2-①). 있으나 마나가 아니라 **작업 시간을 갉아먹는 순손실** | C4로 전면 교체. 교체 전까지는 도구 목록에서 일시 제외 | 타임아웃 0건 |
| **R7** | `DEFAULT_RISKY`에 걸린 `agent_create`/`agent_delete`의 무조건 승인 | 승인 240건 중 **219건(91%)이 이 둘**. 사용자가 팝업 처리기가 됐다(§2-③) | C7의 **정원제(quota)로 대체** — 정원 내 생성·삭제는 자동 허용, 정원 초과·CEO/팀장 삭제만 승인 | 일 평균 승인 팝업 5건 이하 |
| **R10** | HTTPS 리스너 실패가 **서버 전체를 죽인다** | `index.ts`의 HTTPS 리스너는 부가 기능인데 `Bun.serve`가 try 없이 호출돼, 5443 포트가 이미 점유되면 `EADDRINUSE`로 **프로세스가 exit 1** 한다. 실측 사고: 재시작 시도가 이것 때문에 조용히 실패했고, 살아남은 구버전 프로세스가 계속 서비스하면서 **"재시작했는데 코드가 안 바뀐다"**는 상황이 이어졌다. launchd도 같은 이유로 재기동에 반복 실패(`launchctl list` → `- 1`) | `try/catch`로 격리해 HTTPS가 실패해도 HTTP는 계속. 오해를 부르던 "listening" 로그(실제 바인딩 전에 출력됨)도 "시작 중"으로 정정 | ✅ **2026-09-16 완료** |
| **R9** | `tsconfig.json`의 `include: ["server"]` — 봇 작업 디렉터리까지 타입체크 대상 | 봇이 `workspace/`에 쓴 `.ts` 스크래치가 컴파일 대상에 포함돼 **`tsc`가 상시 실패(오류 15건)**했다. 검증 수단 자체가 죽어 있어 "타입체크 통과"가 의미 없는 기준이었다 | `include`를 `server/src`로 좁히고 `server/data` 제외, `lib`에 `DOM.Iterable` 추가 | ✅ **Phase 16에서 완료** — `tsc --noEmit` 오류 0 |
| **R8** | `업무절차서.md`의 "상태: 진행 중 (Phase 0 착수 전)" 헤더 및 Phase 15 이후 미기록 구간 | 문서가 **실제 코드보다 8커밋 뒤처져** 있어 지침서로서 신뢰를 잃었다 | Phase 16에서 Phase 12~15 실체 반영 + 커밋 `35569fd`~`5b06850` 8건 로그 추가 | `git log` 최신 커밋이 절차서 로그 마지막 줄과 일치 |

---

## 4. 추가해야 할 것 (Add)

| ID | 무엇을 | 왜 (그록봇 대비 / 실측 근거) | 구현 위치 |
|---|---|---|---|
| **A1** | **참조 스냅샷(`[ref=N]`) 기반 브라우저 조작** — 페이지의 상호작용 요소(button/a/input/select/[role])를 수집해 번호를 붙여 반환하고, `browser_click("@12")`로 지목 | §2-① 클릭 실패 50%의 **직접 해결**. 모델이 셀렉터를 추측할 필요가 없어진다. ego-lite의 `snapshotText()`가 이미 이 방식이고, 그래서 `ego_run` 39회는 실패 0건이다 | `browser.ts` — `snapshot()` 전면 재작성 |
| **A2** | **테이크오버(사용자 인계) 플로우** — 봇이 2FA/CAPTCHA/결제를 만나면 `browser_handoff` 호출 → 화면에 "지금 인계하세요" 배너 + headed 창 전환 → 사용자 완료 → "반환" 클릭 → 봇 재개 | 그록봇 핵심 기능(§1-⑤). **현재는 2FA 사이트에서 무조건 실패**한다. 사내 그룹웨어·금융·항공 등 실무 대상 대부분이 해당 | `browser.ts` + `ApprovalModal`류 신규 모달 |
| **A3** | **컴퓨터 뷰** — 봇 실행 중 현재 페이지 스크린샷을 2~3초 간격 SSE로 밀어 사이드 패널에 표시 | 그록봇 대비 부재(§1-④). **봇이 뭘 하는지 안 보이면 신뢰도, 디버깅도 불가능**. 실제로 클릭 실패 12건을 사용자가 사후에야 알았다 | `browser.ts` 스크린샷 스트림 + `TeamTrace` 확장 |
| **A4** | **프로바이더 폴백 체인** — 429/5xx 시 동급 모델로 자동 전환(minimax→zai→opencode-zen 등), 전환 사실을 실행 로그에 기록 | 실패 125건 중 **65건(52%)이 429**(§2-②). 9개 프로바이더를 등록해 놓고 활용하지 않는 건 명백한 낭비 | `providers/openaiCompat.ts` + `providers/index.ts` |
| **A5** | **중단 실행 재개(resume)** — 서버 재시작 시 `running` 실행을 error로 죽이지 말고, `agent_runs.tool_log` + 마지막 메시지로 체크포인트를 복원해 재시작 | 재시작 중단 **34건(27%)**이 전량 소실(§2-②). 이미 `checkpointMemory()`가 있으니 그 자산을 살리면 된다 | `index.ts` 부팅 훅 + `team.ts` |
| **A6** | **봇 정원제(quota)** — 전체 봇 상한 20개(그록봇은 50), 팀장당 하위 4개(현행 유지), 정원 내 생성·삭제는 승인 없이 허용, 초과 시에만 승인 | `agent_delete` 594회·승인 거부 87건(§2-③). **한도가 없으니 CEO가 무한 재편**한다. 상한이 있으면 재편 대신 재사용을 택한다 | `team.ts:callBuiltin` + `approvals.ts:gateApproval` |
| **A7** | **터미널 도구 `shell_run`** — `WORK_DIR` 샌드박스 내에서 `bun`/`python3` 실행, 30초 타임아웃, 네트워크 차단, 승인 게이트 대상 | §2-④ — 봇들이 **실행할 수 없는 `.ts`/`.py` 스크립트를 12개나 썼다**. 그록봇은 터미널을 갖는다. 데이터 가공·변환 작업에서 도구 루프 라운드를 대폭 절약 | `server/src/shell.ts` 신규 |
| **A8** | **시연 기반 스킬 학습** — headed 브라우저에서 사용자 조작을 이벤트 후킹(click/input/navigate)으로 최대 10분 녹화 → LLM이 `[적용 조건]/[절차]` 초안 생성 → 사용자 검수 후 `skills` 저장 | 그록봇 "Teach a task"(§1-⑧). 현재 학습 스킬은 **4개 중 1개**뿐 — 글로 쓰는 방식만으로는 축적이 안 된다는 증거 | `browser.ts` 레코더 + `skills` 연동 |
| **A9** | **웹훅 트리거** — `POST /api/hooks/:token` 수신 시 매칭 루틴 발화(Slack Outgoing Webhook, GitHub Webhook, 사내 시스템 공용) | 그록봇 이벤트 트리거(§1-⑩). 현재 이메일(IMAP) 폴링만 있어 **반응 지연 최대 30초 + 메일 없는 이벤트는 불가** | `routines.ts` + 신규 `routes/hooks.ts` |
| **A10** | **루틴 실행 이력 UI** — 루틴별 최근 20건(그록봇 동일)의 성공/실패·소요시간·도구 로그 조회 | 데이터는 `agent_runs`에 이미 쌓이는데 **볼 방법이 없다**(§1-⑪). 루틴이 조용히 실패해도 모른다 | `routines.ts` GET `/:id/runs` + `SettingsModal` |
| **A11** | **MCP remote HTTP 지원** | 그록봇은 remote HTTP만, MyBot은 stdio만 — **서로 반대**라 공용 MCP 서버를 못 쓴다. 양쪽 다 지원해야 생태계에 붙는다 | `mcp.ts` |
| **A13** | `browser_tabs` · `browser_upload` · `browser_download` — **보류** | Phase 17에서 의도적으로 넣지 않았다. 새 창 문제는 도구를 늘리지 않고 **팝업 자동 추적**으로 해소됐고, 업로드·다운로드는 실측 실패 근거가 하나도 없다. 도구가 늘면 모델이 고를 선택지만 늘어 오히려 정확도가 떨어진다. 그룹웨어 첨부 다운로드가 실제로 막히는 사례가 나오면 그때 추가한다 | 보류 — 근거 확보 시 |
| **A12** | **인자 조건 승인 규칙** — `{tool, when: {field, op, value}}` 형태. 예: "`send_email`이면서 `to`가 사내 도메인이 아니면 승인" | 그록봇의 "좁은 규칙" 권고(§1-⑭). 현재는 도구명 정규식뿐이라 **전부 승인 아니면 전부 허용** 두 극단만 가능 | `approvals.ts:approvalDecision` |

---

## 5. 개선·수정·변경해야 할 것 (Change)

| ID | 대상 | 현재 문제 | 변경 내용 | 왜 |
|---|---|---|---|---|
| **C1** | `browser.ts:snapshot()` | `innerText` 8,000자 + 링크 30개. 클릭 가능 요소·폼 구조·현재 상태를 알 수 없음 | A1의 참조 스냅샷으로 교체 + 가시영역 우선 + 폼 필드 라벨·현재값 포함 + iframe 재귀 | 클릭 실패 50%의 근본 원인(§2-①) |
| **C2** | `browser_click`/`browser_type` 대기 전략 | `waitForTimeout(1200~1500)` 고정. 느린 그룹웨어에선 부족, 빠른 페이지에선 낭비 | `waitForLoadState("networkidle")` + `waitForSelector` + 변화 감지. 실패 시 **스냅샷을 함께 반환**해 모델이 즉시 재시도 가능하게 | 실패 12건 전부가 8초 고정 타임아웃(§2-①) |
| **C3** | `browser_click` 실패 메시지 | `Timeout 8000ms exceeded — waiting for locator(…)` 원문만 | 실패 시 **"현재 페이지의 클릭 가능 요소 목록"을 붙여 반환** | 모델이 다음 시도에서 맞는 요소를 고를 수 있다. 재시도 1회로 끝날 일이 3라운드를 먹는다 |
| **C4** | `browser_look` | 풀페이지 PNG → `defaultModelId()`(비전 미지원 가능), 120초 타임아웃 7건 | ① viewport만 캡처 ② 최대폭 1280 리사이즈 ③ **비전 모델 명시 지정**(설정 `vision_model`) ④ 타임아웃 45초 ⑤ 실패 시 텍스트 스냅샷으로 자동 폴백 | 실패 70%, 평균 97초는 순손실(§2-①, R6) |
| **C5** | 브라우저 컨텍스트 수명 | 전역 단일 `ctx`, headless↔headed 전환 시 **전체 재시작**(로그인 세션 유지되나 진행 중 작업 전멸) | 컨텍스트를 유지한 채 **headed 창만 별도 오픈**하거나, CDP 연결로 모드 전환 없이 표시. 최소한 전환 전 진행 중 run 확인 후 대기 | A2 테이크오버의 전제 조건 |
| **C6** | `agent_direct` 시간 예산 | 하위 540초 상한 > 상위 480초 데드라인. 최대 실측 **567초** | **예산 상속** — 하위 상한 = `min(540, 상위 잔여시간 - 60초)`. 잔여 60초 미만이면 위임 거부하고 직접 수행 지시 | 상위가 하위를 기다리다 먼저 죽는 구조(§2-③) |
| **C7** | 승인 게이트 정책 | 도구명 기준 무조건. `agent_create`/`agent_delete`가 전체의 91% | A6 정원제로 전환. 승인 대상은 **① 정원 초과 생성 ② CEO/팀장 삭제 ③ 외부 발신(메일·텔레그램) ④ 결제·SQL·shell_run**으로 한정 | 팝업 240건 중 87건 거부 = 사용자 피로(§2-③, R7) |
| **C8** | CEO 역할 프롬프트(`BOSS_ROLE`) | "조직이 커지면 팀장을 지정하세요" — **조직을 키우라는 유인**이 명시돼 있다 | 조직 **유지·재사용 우선** 원칙으로 재작성: "기존 봇으로 처리 가능하면 새로 만들지 않는다 / 봇 삭제는 사용자가 명시 요청할 때만 / 조직 개편은 업무가 아니다" | `agent_delete` 594회·`agent_list` 730회의 근본 원인이 프롬프트에 있다(§2-③) |
| **C9** | 429 재시도 | 1회 즉시 재시도 후 포기 | 지수 백오프(1s→4s→10s) 3회 + A4 프로바이더 폴백 + `Retry-After` 헤더 준수 | 실패의 52%(§2-②) |
| **C10** | 도구 루프 이중화 | `chat.ts`(4라운드) / `team.ts`(12라운드) 별개 구현 | `server/src/toolloop.ts`로 **단일 엔진 추출**, 라운드 수·데드라인만 파라미터화 | 동작 불일치 버그의 온상(§2-⑤) |
| **C11** | `chat.ts` 자가교정 블록 (100줄 인라인, 정규식 조건 10개) | 조건이 서로 얽혀 수정 시 회귀 발생 | `server/src/selfcheck.ts`로 분리. 각 조건을 `{name, detect(), fixPrompt()}` 규칙 객체로 테이블화 | 유지보수 불가 수준(§2-⑤) |
| **C12** | 승인 후 재개 경로 3중복 | `resumeAgent` / 계정입력 재개 / `dispatchAgentMessage`가 같은 코드를 다르게 반복 | `runAgentDetached(agent, task, origin)` 단일 헬퍼로 통합 | 한 곳만 고치면 나머지가 어긋난다(§2-⑤) |
| **C13** | `renameSync` 무음 실패 (`team.ts` agent_update, agentsRoute PATCH) | `catch {}` — **봇이 기억을 잃어도 아무도 모른다**. 실제 중복 폴더 2쌍 발생 | 실패 시 로그 + 사용자에게 경고 반환. 대상이 이미 있으면 **병합**(양쪽 MEMORY.md 이어붙임) | 실측 사고(§2-④, R2) |
| **C14** | `safePath()` 경로 정규화 | `..` 필터만. NFD/NFC 미처리 | `normalize("NFC")` 추가 + 파일명 길이 제한 | NFD 중복 파일 실측(§2-④, R3) |
| **C15** | 메모리 회상(`recallMemories`) | 최근 300건 LIKE 스캔. 현재 **636건**이라 절반이 스캔 대상 밖 | SQLite FTS5 인덱스 + 중요도·최근성 가중 스코어. 90일 미참조 메모리는 아카이브 | 봇이 오래된 맥락을 잃는다(§2-④) |
| **C16** | 데이터 보존 정책 부재 | `agent_runs` 629 / `approvals` 240 / `messages` 1,497 무한 증가, `browser-profile` 326MB | 일일 정리 루틴: 실행이력 **루틴당 최근 20건**(그록봇 동일)·전체 90일, 처리된 승인 30일, 브라우저 캐시 월 1회 삭제 | DB·디스크 증가와 회상 품질 저하(§2-④) |
| **C17** | 루틴 스케줄러 누락 보정 | `nextRunAt(schedule, last_run_at)` — 서버가 꺼진 동안 지난 `daily` 실행은 **다음 날로 넘어간다** | 부팅 시 미실행 탐지 → 유예(grace) 2시간 내면 즉시 1회 실행 | 맥미니 재부팅 때마다 아침 브리핑이 조용히 누락 |
| **C18** | 봇 복제(`/duplicate`) | 스킬은 복사, **루틴은 미복사** | 루틴도 함께 복제(비활성 상태로) — 그록봇 사양과 일치 | §1-⑲ |
| **C19** | 워크스페이스 → 프로젝트 승격 | 현재는 지침 문자열 주입뿐 | 봇 배정 + 프로젝트 공유 메모리(`memories.workspace_id`) + 프로젝트 파일 네임스페이스 | 그록봇 메모리 3계층·Projects(§1-⑰⑱) |
| **C24** | `chat.ts` 도구 루프가 4라운드 (`team.ts`는 12) | **직접 대화가 위임보다 3배 불리했다.** 사용자가 봇에게 직접 메일 요약을 시킨 3회 모두, 봇이 `list_files`·`skill_list`·`read_file`로 예산을 다 쓰고 **브라우저를 열어보지도 못한 채** 종료됐다. 시간은 별도 deadline(8분)이 막으므로 라운드를 줄일 이유가 없었다 | `team.ts`와 같은 12라운드로 통일 | ✅ **2026-09-16 완료** |
| **C25** | 봇이 자기 `MEMORY.md`를 도구로 다시 읽는다 | 일반 대화에서는 노트가 시스템 프롬프트에 이미 주입되는데 봇이 `read_file`로 또 읽었고, **위임 실행에서는 아예 주입되지 않아** 반드시 찾아 읽어야 했다. 실측 3회 모두 1~6라운드를 노트 탐색에 소모 | `chat.ts`는 "이미 주입됨, 다시 읽지 말 것"을 명시, `team.ts`는 노트를 시스템 프롬프트에 주입 | ✅ **2026-09-16 완료** |
| **C26** | 학습 스킬·봇 메모리가 **구버전 결함의 회피책을 박제**한다 | `그룹웨어-메일조회` 스킬에 "browser_look은 120초 걸리니 ego_run/snapshotText 우선"이 남아 있었다. 엔진을 고쳐도 봇은 스킬을 따라 옛 우회 경로로 간다 — **코드 수정만으로는 개선이 반영되지 않는다.** 봇 MEMORY.md의 실패 기록도 같은 작용을 한다 | 스킬을 `@번호` 기반 절차로 교체, 그룹웨어 4개 봇 메모리 맨 앞에 정정 노트 추가. 일반화: **엔진을 바꾸면 그 엔진을 전제로 저장된 스킬을 함께 재검토**해야 한다 (Phase 22 스킬 성공률 통계와 연결) | ✅ **2026-09-16 완료** |
| **C22** | `default_model`이 CLI 어댑터(`cursor/composer-2.5`) | **CLI 어댑터는 도구가 통째로 비활성이다** (`caps.tools=false`) — 검색·브라우저·파일·위임 어느 것도 못 쓴다. 실측: 이 모델을 쓰는 봇 3개(Eggbot·비서실장봇·개발봇)의 **6회 실행 중 도구 호출 0회**. 게다가 기본 모델이라 **새로 만드는 봇이 전부 도구 없이 태어난다**. 조직을 키워도 일을 못 하는 봇만 늘어난다 | 기본 모델을 도구 가능한 모델로 변경(설정 → 기본 AI 모델). 코드 쪽은 `agent_create`가 모델 미지정 시 도구 가능한 모델을 고르게 하고, CLI 모델 봇에는 목록에 "도구 불가" 배지 표시 | Phase 19 (거버넌스) |
| **C23** | 채팅 이미지 첨부가 CLI 모델 봇에서 조용히 사라짐 | `providers/adapters.ts`의 `textOf()`가 `image_url` 파트를 빈 문자열로 만든다. 사용자가 이미지를 첨부해도 **에러 없이 무시**되고 봇은 "이미지가 안 보인다"가 아니라 엉뚱한 답을 한다 | 첨부가 있는데 대화 봇 모델이 이미지 입력 불가면, 비전 가능 모델로 자동 우회하거나 최소한 사용자에게 알린다 (`browser_look`의 `resolveVisionModel()` 재사용) | Phase 19 |
| **C21** | `checkpointMemory()`의 MEMORY.md 무한 증가 | CEO의 MEMORY.md가 **89KB**인데 시스템 프롬프트는 앞 **1,500자**만 주입한다 — 98%가 읽히지 않으면서 디스크만 차지하고, 봇이 `read_file`로 열면 컨텍스트를 통째로 먹는다 | 체크포인트 상한(최근 N건 또는 20KB) + 초과분은 `MEMORY-archive-YYYY-MM.md`로 롤오버 | Phase 22 (C15 메모리 정리와 함께) |
| **C20** | `browser_login` 성공 판정 | 비밀번호 필드 잔존 여부만 확인 | 성공 URL 패턴·쿠키·특정 요소 존재 등 **사이트별 성공 기준을 `site_logins`에 저장** + 실패 시 A2 테이크오버로 승격 | 2FA 사이트에서 "실패했거나 추가 인증 필요"만 반환하고 끝난다 |

---

## 6. 단계별 작업 TODO

> 원칙: **실측 실패를 줄이는 순서**로 배치했다. Phase 16~18이 전체 효과의 대부분이다.
> 각 Phase는 검증 기준을 통과해야 완료 처리한다.

### Phase 16 — 정리와 기반 정비 `[완료]` ✅ 2026-09-16
**목표**: 오염된 작업공간·분산된 스키마를 정리해 이후 작업의 바닥을 고른다. (예상 0.5일)

- [x] R1 — 작업 디렉터리 루트 정리. 단순 아카이브 대신 주제별로 묶었다: `briefing/`(9) · `morning-briefing/`(8) · `_archive/2026-09/`(스크래치 14 + 이력문서 2). 참조되던 파일은 함께 옮기고 CEO MEMORY.md의 경로 5곳을 갱신
- [x] R2 — 고아 봇 폴더 5개 정리. 살아 있는 봇과 짝이 맞는 3건은 MEMORY.md **끝**에 `[아카이브 병합]` 섹션으로 병합(앞 1,500자만 주입되므로 현재 맥락을 밀어내지 않게), 원본은 `_archive/2026-09/agents-orphan/`에 보존
- [x] R3 + C14 — `safePath()`가 NFC 정규화 + 낱자모(U+1100~U+11FF) 제거 + 기존 NFD 파일 폴백 조회. 파일시스템은 NFD 저장·정규화 무관 조회(APFS)로 실측 확인
- [x] R5 — `skills` 테이블 정의를 `db.ts`로 이관, `routes/workspaces.ts`는 시드만 담당
- [x] R4 — `parseLeaked`를 `server/src/toolloop.ts`로 추출(C10의 첫 단추)
- [x] C13 — `renameAgentFolder()` 신설: 실패를 응답·로그로 드러내고, 대상 폴더가 있으면 덮어쓰지 않고 병합
- [x] C16 — `server/src/maintenance.ts` 신설(루틴당 20건·전체 90일·승인 30일·캐시 월1회). 진행 중 레코드 제외, 브라우저 실행 중이면 캐시 정리 연기
- [x] 추가 — `agent_runs.routine_id` + 인덱스(A10의 전제), tsconfig 정리
- [x] R8 — `업무절차서.md`에 Phase 15+ 미기록 구간 28커밋 반영
- [ ] `업무지침서.md`(이 문서) git 추적 추가 — **보류**: 작업 환경에 연결 폴더 삭제 권한이 없어 `git add`가 `.git/index.lock` 교체 단계에서 실패한다(`Operation not permitted`). 사용자가 로컬에서 `git add 업무지침서.md 업무절차서.md` 실행 필요

**검증**: `list_files()` 루트 20줄 이내 / `ls workspace/agents` == DB 봇 / `tsc` 통과 / 신규 DB로 부팅 시 스키마 오류 0

> 검증 결과 (2026-09-16)
> - 작업 디렉터리 루트 **36개 → 6개** (기준 20 이하 통과)
> - `agents/` 폴더 **11개 → 6개**, 전부 DB 실재 봇 — 고아 **0**
> - 파일 유실 **0건** (이동 전후 basename 전수 대조), 새로 깨진 참조 **0건** (기존 미해결 5건은 이동 이전부터 존재함을 백업 목록으로 확인)
> - 타입체크 **오류 15건 → 0건** — `tsc`가 원래부터 실패 상태였고(봇 스크래치 `.ts`가 체크 대상에 포함), 이번에 검증 수단을 되살렸다
> - 신규 DB 스키마 18개 테이블 생성 성공, `skills.agent_id` 포함, 오류 **0건**
> - 보존 정리 드라이런: 현재 데이터 삭제 **0건**(전부 보존 기간 내), 루틴당 20건 상한 동작 확인(25→20)
> - 브라우저 캐시 회수 예정 **318MB/326MB**, 쿠키·로그인 자산과 경로 겹침 **0건**

---

### Phase 17 — 브라우저 조작 엔진 v2 `[구현 완료 · 실사용 검증 대기]` 2026-09-16 ★최우선
**목표**: 클릭 실패율 50% → 10% 이하. "웹 브라우저 최고 능력자"의 실체를 만든다. (예상 2일)

- [x] A1 + C1 — 참조 스냅샷 구현 (`REF_ATTR`, `collectFrame`, `snapshot`, `locate`)
  - [x] 상호작용 요소 수집: `a[href], button, input, select, textarea, summary, [role=button|link|tab|menuitem|checkbox|radio|option], [contenteditable], [onclick]`
  - [x] 각 요소에 `@N` 부여(DOM 속성 `data-mybot-ref`로 심어 다음 호출에서 정확히 재조회) + 역할·접근가능 이름·현재값·비활성/체크 상태 표기
  - [x] 화면 밖 요소는 `·` 표시, 상한 60개(초과 시 안내 문구), 표시 상한 도달 알림
  - [x] iframe 재귀 수집 — 본문이 iframe 안에 있으면 `[iframe 본문]`으로 덧붙임 (그룹웨어 대응)
  - [x] 페이지 전역 상태: URL·제목·**모달 존재**·**로딩 중 여부**
- [x] `browser_click`/`browser_type`이 `@N` 참조 수용 (CSS·`text=` 셀렉터도 계속 허용 — 기존 스킬 호환)
- [x] C2 — 대기 전략 교체: `settle()` = domcontentloaded + networkidle(상한 2초) + **DOM 변화 정숙 대기**(0.4초 무변화 또는 2.5초 상한). 고정 `waitForTimeout(1200~1500)` 전면 제거
- [x] C3 — 클릭·입력 실패 시 현재 화면의 조작 가능 요소 목록을 함께 반환
- [x] C4 + R6 — `browser_look` 재구현: viewport만 JPEG(q60) 캡처, 45초 상한, 실패 시 텍스트 스냅샷 폴백, **비전 모델 자동 선택**(`resolveVisionModel()` — CLI 어댑터 배제 + 인증된 프로바이더에서 이미지 입력 가능한 모델 선택, `vision_model` 설정은 선택적 override)
- [x] 신규 도구: `browser_wait(text|selector|seconds)`, `browser_back`
- [x] 팝업 추적 — 탭 스택 도입. `target=_blank` 새 창을 자동으로 따라가고 `browser_back`으로 복귀
- [x] `browser_eval` 유지 — 설명에 "@번호 클릭으로 안 되는 경우에만" 명시
- [ ] **실사용 검증** — 다우오피스에서 로그인 → 받은메일함 → 메일 3건 제목 추출을 `browser_eval` 없이 완주(5회 중 4회). **서버 재시작 후 사용자 환경에서만 가능** (이 세션에서는 사내 그룹웨어 세션에 접근할 수 없음)

**검증 기준**: 다우오피스에서 로그인 → 받은메일함 → 메일 3건 제목 추출을 `browser_eval` 없이 완주, 5회 중 4회 이상 성공, `browser_click` 실패율 10% 이하.

> 검증 결과 (2026-09-16) — 격리 환경 실측
> 실제 Chromium(Playwright)에 **browser.ts의 스냅샷 엔진 코드를 그대로 추출·트랜스파일해** 돌렸다. 손으로 옮겨 적은 사본이 아니라 실제 코드다.
> - **그룹웨어 재현 픽스처**(모달 · 지연 렌더 · iframe 목록 · `target=_blank`) 시나리오 **15개 항목 전부 통과**. 이 픽스처는 실측 실패 로그를 그대로 본떴다 — `text=다시 보지 않기` 타임아웃, `text=받은메일함` 타임아웃, iframe 내부 목록, 새 창으로 열리는 문서.
> - 노출 요소 수: 구버전(메인 프레임 링크만) **3개 → 신버전 11개**. 모달 버튼·검색창·iframe 메일 행이 전부 구버전에선 보이지 않던 것들이다.
> - 실사이트 부하·성능: example.com / wikipedia.org / news.ycombinator.com에서 스냅샷 생성 **10~19ms**, 출력 263~5,826자, 오탐(로딩·모달 경고) **0건**.
> - **테스트가 버그 3개를 잡아냈다**: ① `offsetParent`로 가시성을 판정해 `position:fixed` 모달을 "숨김"으로 오판 → rect+computed style 판정으로 교체 ② 렌더 완료 전에 읽어 "조작 가능 요소 없음"을 반환 → 로딩 상태 감지 추가 ③ 고정 시간 재시도가 64ms 모자라 실패 → **변화가 멈출 때까지 수렴 폴링**(총예산 6초, 2회 연속 동일하면 중단)으로 교체
> - 타입체크 오류 0
>
> **`browser_look` 실패 70%의 진짜 원인을 찾았다.** 처음엔 "비전 지원 여부도 모르는 기본 모델"이라고 적었는데, 설정을 실제로 열어 보니 더 구체적이었다 — 이 맥의 `default_model`이 `cursor/composer-2.5`, 즉 **CLI 어댑터**다. `providers/adapters.ts`의 `textOf()`는 프롬프트를 텍스트로 직렬화하면서 `image_url` 파트를 빈 문자열로 만든다. 즉 스크린샷은 **전송조차 되지 않은 채** 버려졌고, `cursor-agent`는 "이 화면을 설명해줘"라는 이미지 없는 프롬프트를 받고 120초 타임아웃까지 매달려 있었다. 설정 항목을 하나 더 만들어 사용자에게 떠넘기는 대신, 이미지 입력이 실제로 가능한 모델을 서버가 자동으로 고르게 했다(이 환경에서는 `openai/gpt-6-astra`가 선택된다 — CEO가 이미 쓰는 인증된 모델).

---

### Phase 18 — 실행 안정성 `[대기]` ★최우선
**목표**: 실행 실패율 19.9% → 8% 이하. (예상 1.5일)

- [ ] A4 — 프로바이더 폴백 체인
  - [ ] 설정에 `fallback_chain` (예: `minimax → zai → opencode-zen`)
  - [ ] 429/5xx/잔액부족(1113) 시 자동 전환, 전환 사실을 `tool_log`와 화면에 표기
- [ ] C9 — 지수 백오프 3회 + `Retry-After` 준수
- [ ] A5 — 중단 실행 재개
  - [ ] 부팅 시 `running` 실행을 error로 죽이지 말고 `resumable`로 표시
  - [ ] `checkpointMemory()` + `tool_log`로 컨텍스트 복원 후 재실행
  - [ ] 3회 재개 실패 시에만 error 확정
- [ ] C6 — 위임 시간 예산 상속(`min(540, 상위잔여-60)`)
- [ ] C17 — 스케줄러 누락 보정(유예 2시간)
- [ ] 데드라인·라운드 수를 설정값으로 노출(현재 하드코딩 8분/12라운드/4라운드)

**검증**: minimax 429 강제 유발 시 자동 폴백으로 작업 완주 / 실행 중 서버 재시작 후 자동 재개 확인 / 1주일 운영 후 `agent_runs` 실패율 8% 이하

---

### Phase 19 — 조직 거버넌스 `[대기]`
**목표**: 승인 팝업 일 5건 이하, 조직 관리 도구 호출 비중 50% 이하. (예상 1일)

- [ ] A6 + C7 — 봇 정원제
  - [ ] 전체 상한 20, 팀장당 4(현행), 설정에서 조정 가능
  - [ ] 정원 내 생성·삭제는 승인 면제
  - [ ] 승인 대상을 ①정원초과 ②CEO/팀장 삭제 ③외부 발신 ④결제·SQL·`shell_run`으로 한정
- [ ] C8 — `BOSS_ROLE` 재작성(조직 유지·재사용 우선, "조직 개편은 업무가 아니다" 명시)
- [ ] A12 — 인자 조건 승인 규칙(`{tool, when:{field,op,value}}`)
- [ ] `agent_list` 호출 캐싱 — 같은 실행 내 30초 재사용 (730회 → 대폭 감소)
- [ ] 감사 뷰 — 설정에 "봇 활동 이력"(봇·도구·기간 필터, `agent_runs` + `approval_requests` 통합)
- [ ] C18 — 봇 복제 시 루틴 포함

**검증**: 1주 운영 후 `agent_delete` 호출 일 10건 이하 / 승인 팝업 일 5건 이하 / 거부율 20% 이하

---

### Phase 20 — 컴퓨터 뷰 + 테이크오버 `[대기]`
**목표**: 2FA·CAPTCHA 사이트에서 작업 완주 가능. (예상 2일)

- [ ] C5 — 브라우저 컨텍스트 수명 재설계(headed 전환 시 진행 작업 보존)
- [ ] A3 — 컴퓨터 뷰
  - [ ] 봇 실행 중 2~3초 간격 viewport 스크린샷 SSE 푸시
  - [ ] `TeamTrace` 옆 "봇 화면" 패널 + 현재 URL·최근 액션 표시
  - [ ] 성능 보호: 최대 폭 960, JPEG 60%, 보는 사람이 있을 때만 캡처
- [ ] A2 — 테이크오버
  - [ ] `browser_handoff(reason)` 도구 — 봇이 2FA/CAPTCHA/결제 감지 시 호출
  - [ ] 화면에 인계 모달 + headed 창 포그라운드
  - [ ] 사용자 "완료" → 봇 자동 재개(승인 재개 경로 재사용)
  - [ ] 5분 무응답 시 부분 보고로 마무리
- [ ] C20 — `browser_login` 성공 기준을 `site_logins`에 저장, 실패 시 테이크오버로 승격
- [ ] C12 — 재개 경로 3중복을 `runAgentDetached()`로 통합

**검증**: 2FA가 걸린 실제 사이트에서 로그인 → 인계 → 반환 → 봇이 데이터 수집 완료까지 1회 완주

---

### Phase 21 — 도구 루프 통합 + 터미널 `[대기]`
**목표**: 유지보수 가능한 단일 실행 엔진. (예상 1.5일)

- [ ] C10 — `toolloop.ts` 단일 엔진 추출(`chat.ts`/`team.ts` 양쪽 대체)
- [ ] C11 — `selfcheck.ts` 분리, 조건 10개를 규칙 객체 테이블로
- [ ] A7 — `shell_run` 도구
  - [ ] `WORK_DIR` 샌드박스, 30초 타임아웃, 네트워크 차단, 출력 8,000자 상한
  - [ ] 승인 게이트 대상(기본 require)
  - [ ] 허용 인터프리터 화이트리스트(`bun`, `python3`, 기본 유닉스 유틸)
- [ ] 도구 설명에 "스크립트를 쓰면 `shell_run`으로 실행하라" 명시

**검증**: `tsc` 통과 / 기존 시나리오(팀 위임·루틴·그룹챗) 회귀 없음 / 봇이 CSV 가공을 `write_file` + `shell_run`으로 1라운드에 완료

---

### Phase 22 — 스킬 학습(시연) `[대기]`
**목표**: 반복 업무가 사람 개입 없이 스킬로 축적된다. (예상 2일)

- [ ] A8 — 시연 레코더
  - [ ] headed 창에 이벤트 후킹 주입(click/input/change/navigate/submit)
  - [ ] 최대 10분, 비밀번호 필드는 `[비밀값]`으로 마스킹(그록봇 동일 원칙)
  - [ ] 녹화 종료 → LLM이 `[적용 조건]/[절차]/[주의]` 초안 생성
  - [ ] 사용자 검수·편집 후 `skills` 저장
- [ ] 스킬에 **참조 스냅샷 기반 절차** 저장(Phase 17 산출물 활용 — 셀렉터가 아니라 요소 설명으로)
- [ ] 스킬 실행 통계(`skill_runs`): 사용 횟수·성공률·최근 실패 사유
- [ ] 성공률 50% 미만 스킬은 자동 비활성 + 재학습 제안
- [ ] C15 — 메모리 FTS5 + 가중 스코어 + 90일 아카이브

**검증**: 그룹웨어 메일 브리핑을 시연 1회로 스킬화 → 다음 실행이 도구 라운드 30% 이상 감소

---

### Phase 23 — 트리거 확장 + 프로젝트 `[대기]`
**목표**: 그록봇 기능표 전 항목 ○ 또는 △ 이상. (예상 1.5일)

- [ ] A9 — 웹훅 트리거(`POST /api/hooks/:token`), 토큰 발급 UI, 매칭 규칙(발신·본문 키워드)
- [ ] Slack Outgoing Webhook / GitHub Webhook 프리셋
- [ ] A10 — 루틴 실행 이력 UI(루틴당 최근 20건, 성공/실패·소요·도구 로그)
- [ ] 루틴 한도: 봇당 50개(그록봇 동일)
- [ ] A11 — MCP remote HTTP 지원(stdio와 병행)
- [ ] C19 — 워크스페이스 → 프로젝트 승격(봇 배정 + 프로젝트 공유 메모리 + 파일 네임스페이스)
- [ ] 봇 구성 내보내기/가져오기 JSON (그록봇 "봇 공유"의 셀프호스팅 대응)

**검증**: Slack 메시지로 루틴 발화 확인 / 루틴 이력 화면에서 최근 20건 조회 / §1 대조표 재작성 시 ✕ 0개

---

## 7. 문서 갱신 규칙 (필수 준수)

각 Phase를 완료하면 **같은 커밋 안에서** 이 문서를 다음과 같이 갱신한다.

1. **상태 마킹** — 해당 Phase 제목의 `[대기]`를 `[완료] ✅ YYYY-MM-DD`로 바꾸고, 미완 항목이 있으면 `[진행 중]`으로 두고 남은 체크박스를 명시한다.
2. **체크박스** — 실제로 구현·검증된 항목만 `[x]`. **검증 기준을 통과하지 못한 항목은 체크하지 않는다.**
3. **검증 결과 기록** — Phase 말미에 `> 검증: <실제 측정값>` 한 줄을 추가한다. 예: `> 검증: browser_click 실패율 50% → 7% (실측 41회 중 3회)`
4. **§1 대조표 갱신** — 해당 기능의 판정(✕/△/○)을 올린다.
5. **§2 실측 진단 재측정** — Phase 18·19 완료 후에는 §2의 수치를 **다시 집계해 갱신**한다(수치 옆에 `(2026-mm-dd 재측정)` 표기).
6. **§8 진행 로그에 1줄 추가** — 날짜 / Phase / 무엇을 했고 무엇이 달라졌는지.
7. **새 문제 발견 시** — §3~§5의 표에 새 ID(R9, A13, C21 …)로 추가한다. 발견 사실을 지우지 않는다.
8. **`업무절차서.md`도 함께 갱신** — 절차서는 "무엇을 했는가"의 정사(正史)다. 지침서의 Phase 완료는 절차서에도 한 줄 남긴다.
9. **되돌린 결정도 기록** — 넣었다가 문제가 생겨 걷어낸 것은 §3에 `[철회]` 항목으로 남기고 이유를 적는다. 같은 실수를 두 번 하지 않기 위함이다.

---

## 8. 부록

### 8-A. 진행 로그

| 날짜 | Phase | 내용 |
|---|---|---|
| 2026-09-16 | — | 코드 전수 분석 + 운영 DB 실측 + 그록봇 공식 문서 대조. 업무지침서 v1 작성 |
| 2026-09-17 | 16·17 | **신버전 가동 확인.** 포트 점유 구버전 프로세스 정리 후 launchd 재기동 성공. 확정 근거 3종: `/api/settings`에 `vision_model` 키 노출(29→30), 보존 정리 실행(`maintenance_last_run_at` 기록), 브라우저 캐시 **330MB → 8.9MB**(쿠키·로그인 세션 보존). Phase 16 C16 검증 완료 — 실측 회수량이 예상 318MB와 일치 |
| 2026-09-17 | — | 측정 방법 교훈: 운영 DB 확인 시 `mybot.db`만 복사하면 **WAL에 남은 최신 쓰기를 못 본다**. 실제로 정리 루틴이 돌았는데도 "(없음)"으로 오판했다. 이후 DB 확인은 `-wal`·`-shm`을 함께 복사하거나 서버 API로 조회한다 |
| 2026-09-16 | 17+ | 재시작 실패 원인 규명: 5443 포트를 쥔 **구버전 프로세스가 pkill 패턴(`server/src/index.ts`)에 안 걸려 생존** → launchd 재기동과 수동 `bun start` 모두 EADDRINUSE로 죽고, 구버전이 계속 서비스. 부가 기능인 HTTPS 리스너가 본체를 죽이는 구조가 원인(R10, 수정 완료). 확정 근거: `/api/settings`에 신버전에서 추가한 `vision_model` 키가 노출되지 않음 |
| 2026-09-16 | 17+ | 사용자 메일 요약 실패 3회 원인 분석 및 후속 수정: ① `chat.ts` 도구 루프 4→12라운드(C24) ② `MEMORY.md` 중복 읽기 억제 — team.ts에 노트 주입 추가(C25) ③ 낡은 스킬을 `@번호` 절차로 교체 + 그룹웨어 4개 봇 메모리에 정정 노트(C26). 스킬·메모리는 재시작 없이 즉시 반영, 코드는 재시작 필요. **재시작은 실패** — device_bash는 맥이 아닌 별도 리눅스 VM, 터미널은 클릭 전용 등급, 활성 상태 보기 권한 요청 3회 거부 |
| 2026-09-16 | 17 | `browser_look` 근본 원인 규명: 기본 모델이 CLI 어댑터(`cursor/composer-2.5`)라 스크린샷이 전송 전에 버려지고 있었음. 비전 모델 자동 선택 도입(설정 불필요). 파생 발견 2건 등재 — C22(CLI 기본 모델이라 새 봇이 도구 없이 태어남, 실측 도구호출 0회), C23(이미지 첨부 무음 폐기) |
| 2026-09-16 | 17 | 브라우저 조작 엔진 v2: `@번호` 참조 스냅샷(iframe 재귀·모달/로딩 감지), `settle()` 수렴 대기, 실패 시 요소목록 동봉, 팝업 자동 추적 + `browser_back`, `browser_wait` 신설, `browser_look` 재구현(viewport JPEG·`vision_model`·45초·텍스트 폴백). 격리 Chromium 실측 15/15 통과, 노출 요소 3→11개. 테스트가 버그 3건(가시성 오판·조기 읽기·재시도 부족) 적발해 수정. 실사용 검증은 서버 재시작 후 |
| 2026-09-16 | 16 | 정리·기반 정비 완료. 작업공간 36→6항목, 고아 봇 폴더 5개 병합·보존, skills 스키마 단일화, `toolloop.ts`/`maintenance.ts` 신설, `renameAgentFolder`로 무음 실패 제거, `safePath` NFC 정규화, `agent_runs.routine_id` 추가. 타입체크 15→0. 신규 발견 2건(R9·C21) 등재. git 추적만 권한 문제로 보류 |

### 8-B. 실측 집계 재현 방법

```bash
# 도구별 호출/실패 집계
python3 - <<'PY'
import sqlite3, json, collections
c = sqlite3.connect("server/data/mybot.db")
tot, fail = collections.Counter(), collections.Counter()
for (tl,) in c.execute("SELECT tool_log FROM agent_runs WHERE tool_log IS NOT NULL"):
    for l in json.loads(tl or "[]"):
        tot[l["tool"]] += 1
        if not l.get("ok"): fail[l["tool"]] += 1
for t, n in tot.most_common():
    print(f"{t:22s} {n:5d}  실패 {fail[t]:4d} ({fail[t]*100//max(n,1)}%)")
PY

# 실행 실패 사유
sqlite3 server/data/mybot.db "SELECT substr(result,1,80), COUNT(*) FROM agent_runs WHERE status='error' GROUP BY 1 ORDER BY 2 DESC;"

# 승인 요청 분포
sqlite3 server/data/mybot.db "SELECT tool, status, COUNT(*) FROM approval_requests GROUP BY 1,2 ORDER BY 3 DESC;"
```

### 8-C. 기준선 측정값 (2026-09-16)

| 지표 | 값 | 목표(Phase 23 완료 시) |
|---|---:|---:|
| 타입체크 오류 (`tsc --noEmit`) | 15 → **0** (Phase 16 완료) | **0 유지** ✅ |
| 실행 실패율 (`agent_runs`) | 19.9% (125/629) | **8% 이하** |
| `browser_click` 실패율 | 50% (12/24) | **10% 이하** |
| `browser_look` 실패율 | 70% (7/10) | **10% 이하** |
| 429로 인한 실패 | 65건 (실패의 52%) | **0건** (폴백 흡수) |
| 재시작 소실 실행 | 34건 | **0건** (재개) |
| 조직관리 도구 호출 비중 | 1,587 / 3,465 = **46%** | **20% 이하** |
| 승인 팝업 (누적) | 240건 (거부 87 = 36%) | **일 5건 이하, 거부율 20% 이하** |
| 봇 수 vs 작업폴더 수 | 8 vs 12 (고아 5) → **고아 0** (Phase 16 완료) | **일치** ✅ |
| `workspace/` 루트 항목 수 | 36 → **6** (Phase 16 완료) | **20 이하** ✅ |

### 8-D. 참고 자료

- [Grok Bot — Create and manage Bots (docs.x.ai)](https://docs.x.ai/grok-bot/bots)
- [Grok Bot — Skills, routines and automations (docs.x.ai)](https://docs.x.ai/grok-bot/skills-routines-and-automations)
- [Grok Bot — Approvals, security and privacy (docs.x.ai)](https://docs.x.ai/grok-bot/approvals-security-and-privacy)
- [Grok Bot — Use cases (docs.x.ai)](https://docs.x.ai/grok-bot/use-cases)
- [Grok Bot 해설: SpaceXAI의 상시 가동 AI 에이전트 (DataCamp)](https://www.datacamp.com/blog/grok-bot)
- [그록봇 가격·설치·슬랙 연동 총정리 (GPTers)](https://www.gpters.org/news/post/grok-bot-guide-price-setup-5JRJexbz1E9YgB2)
