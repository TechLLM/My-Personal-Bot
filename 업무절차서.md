# MyBot 업무절차서

> 그록(Grok) 동급 AI 어시스턴트 — 모델 자유 연결형, 맥미니 셀프호스팅
> 작성: 2026-09-14 · 최종 갱신: 2026-09-16 / 상태: **Phase 16 완료**
> 이 문서는 "무엇을 해왔는가"의 기록이다. 앞으로 할 일은 `업무지침서.md`(Phase 17~23)를 본다.

## 1. 목표와 원칙

**목표**: 이 맥미니에서 서비스로 돌아가는 그록과 동일한 UX의 AI 어시스턴트.
유일한 차이점: 특정 벤더 모델에 묶이지 않고 **원하는 모델을 자유롭게 연결**.

**그록 장점 (가져올 것)**
- 스트리밍 채팅, 대화 히스토리, 모델 선택기
- DeepSearch: 쿼리 분해 → 병렬 웹검색 → 출처 읽기 → 인용 포함 종합 (진행상황 가시화)
- Think: 추론 과정을 접이식 패널로 표시
- 이미지 생성/편집, 이미지 업로드 분석
- 페르소나(Fun 모드), 커스텀 에이전트, 메모리
- 워크스페이스(프로젝트별 컨텍스트), 스킬(/명령), 루틴(예약 실행)
- Latest News / 실시간 정보에 강함

**그록 단점 (제거/보완)**
- xAI 모델 고정 → **프로바이더 추상화로 모든 모델 연결** (airoute 기본 + OpenAI호환 직결 + Ollama)
- X 계정/구독 종속 → 로컬 인증 1개, 구독 없음
- 데이터 외부 유출 → 전부 로컬 SQLite, 검색도 셀프호스팅 가능
- 로컬 모델 불가 → Ollama/LM Studio/MLX 지원
- 도구 확장 제한 → MCP 도구 연동(후순위)
- 폐쇄 소스 → 전체 소스 이 디렉토리에 보유

## 2. 확정된 의사결정 (2026-09-14 사용자 승인)

| 항목 | 결정 |
|---|---|
| 플랫폼 | 웹앱 — 맥미니 서비스로 상시 실행, LAN 내 모든 기기 브라우저 접속, PWA |
| 구현 | 직접 개발. 모델 백엔드는 **airoute(127.0.0.1:11441)를 기본 프로바이더**로 사용 |
| MVP | 채팅+모델전환+스트리밍, DeepSearch, Think 표시, 이미지 생성/분석 |
| 후순위 | 페르소나+메모리 → 워크스페이스+스킬 → 에이전트+루틴 → 음성 대화 |

## 3. 기술 스택

- **런타임**: Bun 1.4 (airoute와 동일 스택, SSE/WS/SQLite 내장)
- **백엔드**: Hono (Web 표준 라우터) + bun:sqlite (WAL)
- **프론트엔드**: Vite + React + Tailwind (그록식 다크 미니멀 UI)
- **모델 레이어**: OpenAI-호환 클라이언트 → 기본 대상 airoute `/v1` (600+ 모델, 80% 쿼터 자동전환, 폴백 내장). 추가 OpenAI호환 엔드포인트/Ollama를 설정으로 등록 가능
- **검색 레이어**: 검색 프로바이더 인터페이스 — SearXNG(셀프호스트) / Tavily / Brave / Serper / 내장 DuckDuckGo-lite 폴백. 페이지 리더(HTML→텍스트 정제)
- **이미지**: OpenAI Images-호환 엔드포인트 + 맥 로컬 Draw Things/Stable Diffusion API (플러거블)
- **배포**: launchd 서비스, 기본 포트 5274(5273 autoblog와 구분), 간단한 접속 암호

## 4. 단계별 절차 (각 단계 완료 시 상태 갱신)

### Phase 0 — 스캐폴드 `[완료]` ✅ 2026-09-14
- [x] bun 프로젝트 생성, server/(Hono) + web/(Vite+React) 구조
- [x] SQLite 스키마: conversations, messages(분기 지원), settings, personas, memories, workspaces, routines, files
- [x] dev 스크립트(`bun run dev` → scripts/dev.ts 양쪽 기동), 빌드 스크립트
- 검증 통과: `/api/health` 200, 서버 :5274 / vite dev :5275

### Phase 1 — 채팅 코어 `[완료]` ✅ 2026-09-14
- [x] 프로바이더 레이어: airoute 기본(토큰은 `~/.config/airoute/config.json`에서 자동 독출, 소스에 하드코딩 없음) + 커스텀 OpenAI호환 엔드포인트 등록/삭제 (설정 UI)
- [x] 모델 목록 API — airoute 210개 모델 + 가상 라우트(main/fast/subagent/team/plan/design/code/review/critique) 최상단 표시
- [x] 모델 피커 2단 구조(2026-09-15 개선): 프로바이더 탭(전체/airoute/커스텀엔드포인트) → 네임스페이스 그룹(`/` 접두사, 없으면 `-` 접두사, 1개짜리는 "기타" 병합) 접이식 → 상세 모델. 검색 필터, THINK/EYE/AUTO 배지
- [x] 대화 CRUD, 메시지 SSE 스트리밍, 중단(abort), 재생성(형제 분기), 편집→새 분기, 형제 탐색(‹ 1/2 ›), 자동 제목 생성(fast 모델)
- [x] 그록식 UI: 좌측 대화 목록, 중앙 대화, 하단 통합 프롬프트바(모델 선택+DeepSearch/Think/이미지 칩)
- 검증 통과: 실제 스트림 수신, 토큰 카운트, 자동 제목("한국어 인사와 모델 확인") 확인

### Phase 2 — Think 추론 표시 `[완료]` ✅ 2026-09-14
- [x] `reasoning_content` + `reasoning` 필드 + `<think>` 태그 인라인 파싱 (openaiCompat.ts)
- [x] 접이식 "🧠 사고 과정" 패널 (스트리밍 중 점선 펄스, 완료 후 접기)
- 검증 통과: glm-5.3-flash의 reasoning_content가 별도 이벤트로 수신·저장(rlen 449)

### Phase 3 — DeepSearch `[완료]` ✅ 2026-09-14
- [x] 검색 프로바이더 인터페이스: Bing HTML(기본, 키 불필요) / SearXNG / Tavily / Brave / DDG(차단 잦아 최후순위)
- [x] 에이전트 루프: 쿼리 분해(LLM) → 병렬 검색 → 상위 페이지 fetch/본문 정제 → 스크래치패드 → 추가 검색 필요 판단 → 반복(상한 4라운드/10페이지) → [n] 인용 종합
- [x] 진행상황 이벤트 스트림 + SearchTrace 패널(계획 쿼리 칩, 검색/읽은 출처 타임라인)
- [x] bing ck/a 리다이렉트 base64 디코딩(`&amp;` 이스케이프 처리)
- 검증 통과: "2026 맥미니 스펙" 질의 → 11회 검색/10페이지 정독/인용 리포트, 자료 부족 시 미지 정보 명시(환각 차단)
- 잔여 개선점: Bing 결과 품질이 주제에 따라 범용 페이지 위주 → SearXNG/Tavily 키 설정 시 품질 향상

### Phase 4 — 이미지 `[완료]` ✅ 2026-09-14
- [x] 이미지 생성: OpenAI Images-호환 엔드포인트 + Draw Things 로컬 API(:7888) 자동 분기, 생성물을 server/data/files에 저장 후 /api/files로 서빙
- [x] 프롬프트바 "이미지" 모드 칩
- [x] 이미지 업로드 → 비전 분석: 첨부 버튼📎, 업로드 API, data URL로 비전 모델에 주입
- [x] 텍스트 파일(txt/md 등) 첨부 → 내용 자동 주입 (RAG-lite)
- 참고: 이미지 생성은 설정에서 image_endpoint 지정 필요 (Draw Things 앱 또는 Images 호환 API)

### Phase 5 — 페르소나 + 메모리 `[완료]` ✅ 2026-09-14
- [x] 페르소나: 기본/Fun/번역가/코드리뷰어/선생님 5종 시드 + CRUD + 프롬프트바 선택기, 대화별 저장
- [x] 메모리: 교환 후 fast 모델로 사실 자동 추출 → memories 테이블 → 시스템 프롬프트 주입, 설정에서 조회/삭제/끄기
- [x] 접속 암호(access_code): 설정 시 /api 전체 보호, 프론트는 자동 prompt로 키 입력(localStorage 저장)
- 검증 통과: /api/personas 5종 확인, 추출 파이프라인 코드 검증

### Phase 6 — 워크스페이스 + 스킬 `[완료]` ✅ 2026-09-14
- [x] 워크스페이스 CRUD + 대화 할당 + 사이드바 필터 + 지침을 시스템 프롬프트에 주입
- [x] 스킬: `/요약 /번역 /리뷰` 시드 + CRUD + 입력창 `/` 자동완성 + 서버 측 프롬프트 확장
- [x] 파일 업로드 텍스트 주입 (pdf는 텍스트 추출 미지원 — 텍스트 파일만)
- 검증 통과: `/번역 hello world` → 스킬 프롬프트 확장 후 정상 번역 응답 확인

### Phase 7 — 에이전트 + 루틴 `[완료]` ✅ 2026-09-14
- [x] 루틴: `every:Nm/Nh`, `daily:HH:MM` 스케줄 → 백그라운드 실행 → 결과를 ⏰대화로 자동 저장, 설정에서 관리/지금실행/토글
- [x] MCP: stdio JSON-RPC 클라이언트(`server/src/mcp.ts`), tools/list+call, OpenAI tools 형식 변환, 최대 4라운드 도구 루프 후 최종 스트리밍. 설정 `mcp_servers` JSON으로 서버 등록
- 검증 통과: 루틴 수동 실행 → ⏰대화 생성. MCP는 코드 검증(tsc), 실 MCP 서버 연결 테스트는 설정 후 확인 필요

### Phase 8 — 음성 대화 `[완료]` ✅ 2026-09-14
- [x] STT: 브라우저 Web Speech API (마이크 버튼, ko-KR, interim results) — 무설치
- [x] TTS: speechSynthesis로 어시스턴트 메시지 읽기 (🔊 버튼)
- 참고: 브라우저 엔진 사용 — whisper.cpp/OpenAI 오디오 API로 교체 가능하지만 현재는 무설치 경로
- 검증: Chrome/Safari에서 마이크 권한 허용 후 테스트 필요

### Phase 9 — 패키징 `[완료]` ✅ 2026-09-14
- [x] 단일 포트 서빙: :5274가 API+정적 web/dist 동시 서빙
- [x] 접속 암호(access_code), PWA manifest + icon.svg
- [x] launchd `ai.mybot.server` 등록 완료 — 로그인 시 자동 기동 + KeepAlive. 로그: `~/Library/Logs/mybot/`
- 참고: launchd 로그 경로는 반드시 내장 볼륨 — 외장 볼륨 경로는 spawn 실패(EX_CONFIG 78)
- 검증 통과: launchctl list 정상(pid 존재), /api/health 200

### Phase 10 — 대장 봇(팀 오케스트레이션) `[완료]` ✅ 2026-09-15
- [x] `team` 모드: 대장 봇이 작업을 분해 → 역할 봇 병렬 실행(최대 4개) → 보고서 취합 → 대장이 최종 종합 답변
- [x] 상주 역할 봇: `agents` 테이블, 이름 기준 재사용(다음 팀 작업에서 같은 봇 재활용), 설정 UI에서 직접 생성/삭제/역할·모델 지정
- [x] 봇 도구: web_search + 공유 작업 디렉터리 파일 읽기/쓰기(`server/data/workspace` 샌드박스) + MCP 도구 전체, 최대 8라운드 도구 루프
- [x] `agent_runs` 실행 이력 + SSE `team` 이벤트(계획/시작/도구호출/완료) + TeamTrace 패널(봇 카드·상태·결과 펼치기)
- [x] 대화 재로딩 시에도 팀 결과 표시 (search_meta `{type:"team"}` 저장)
- 검증 통과: "맥미니 뉴스 조사+보고서" 지시 → 🔎조사봇·📝작성봇 자동 생성/실행(web_search·list_files 도구 호출) → 대장이 한계까지 솔직히 종합
- [x] 페르소나 적합성 판단(2026-09-15 추가): 계획 프롬프트에 상주 봇 명단(이름/역할/모델) 제공 → 대장이 재사용(`{"agent":"이름"}`) 또는 새 페르소나 생성을 판단. 역할이 안 맞으면 같은 이름도 신규 생성(유일 이름 자동 부여)
- [x] 루틴 담당 봇 제외: `routines.agent_id`로 봇에 예약 업무 배정 → 활성 루틴 보유 봇은 [바쁨] 표시 후 재사용 풀에서 제외, 대장이 새 봇 생성. 루틴 실행 시 배정 봇의 역할·모델로 수행
- 검증 통과: 루틴 배정된 🔎맥미니봇 제외 → 🔬AI반도체 뉴스 조사 봇 신규 생성, 적합한 📝작성봇은 재사용 확인

### Phase 11 — 내장 브라우저(ego lite 증류) `[완료]` ✅ 2026-09-15
- [x] `server/src/browser.ts`: 영속 프로필(`server/data/browser-profile`)의 Chromium을 MyBot 서버가 직접 구동 — 별도 브라우저 설치 불필요. 봇 작업은 **headless**(Chrome for Testing의 new headless, 창 안 뜸), 수동 로그인만 잠시 headed로 전환 후 복귀
- [x] 로그인 세션 공유: 설정 → "로그인 창 열기"로 맥미니 화면에 창을 띄워 사용자가 수동 로그인(x.com 등) → 쿠키가 프로필에 영속 저장 → 봇이 재사용 (비밀번호를 봇에 직접 주지 않는 안전한 방식)
- [x] 봇 탐지 대응: `--enable-automation` 플래그 제거, `--disable-blink-features=AutomationControlled`, `navigator.webdriver` 숨김, plugins/languages/permissions 위장, ko-KR/Asia-Seoul/실제 뷰포트 — 실제 Chrome 지문과 동일한 환경
- [x] 봇 도구: `browser_open/read/click/type/scroll` — 팀 모드 역할 봇 + 일반 채팅(도구 루프) 양쪽에서 사용 가능, 봇별 탭(page) 격리(ego의 Space에 해당), 실행 후 탭 자동 정리
- [x] `/api/browser/status|open|close` 엔드포인트 + 설정 UI
- 검증 통과: 브라우저 창 실제 기동 확인, 일반 채팅에서 `browser_open` 호출 → example.com 실제 제목 응답 확인
- 주의: X 등은 실세션+실브라우저라도 이용약관상 자동화 스크래핑 제한 있음 — 계정 제재 리스크는 사용자 책임


### Phase 15+ — 미기록 구간 (2026-09-15~16, 커밋 28건) `[완료]` ✅ 2026-09-16
Phase 15 이후 커밋 28건이 이 절차서에 기록되지 않은 채 쌓여 있었다(`23224c4`~`5b06850`). 실제로 만들어진 것은 아래와 같다.

**(1) 그록봇 협업 기능 5종** — `35569fd`
- [x] 승인 경계: `approval_requests`/`approval_rules`, require > allow 우선순위, 기본 위험 패턴(`DEFAULT_RISKY`)
- [x] 비동기 핸드오프: `agent_messages` + `dispatchAgentMessage` — 보낸 봇은 기다리지 않고 회신이 세션에 기록
- [x] 병렬 위임: `agent_direct`의 `names` 배열 + 같은 배치의 위임 호출 동시 실행
- [x] 그룹채팅: `groups` 테이블 + `@멘션`으로 특정 봇 지정
- [x] 이벤트 트리거: `routines.trigger_type='email'` — IMAP 폴링으로 메일 도착 시 루틴 발화

**(2) 검증 하네스** — `cb87981` `1c3e3c2` `7137078`
- [x] `intent.ts`: 지시 의도 → 도구 계약(`TOOL_CONTRACT`) → DB 실측 검증
- [x] 실행 전 `snapshot()` 주입("이 데이터만이 사실"), 실행 후 `verifyMutation()`으로 이행 확인
- [x] 조회 지시 중 일어난 변경은 `undoUnrequestedChanges()`로 되돌림
- 사고 기록: 정규식 의도 분류가 "삭제를 담당하는 봇"(역할 서술)을 "전부 삭제"(명령)로 오독해 **봇 전체 삭제를 강제**했다 → `classifyIntent()` LLM 분류로 재설계, 분류 실패 시 동사를 버리는 보수적 폴백 적용

**(3) PGE 루프** — `07f3c85`
- [x] `evaluate.ts`: 이행·근거·완결·명확 4항목 루브릭(각 25점), 70점 미만이면 지적사항과 함께 재작업(최대 2회)
- [x] 단계 타임라인 UI: 지시 분석 → 작업 실행 → 결과 검증 → 답변 생성 → 완료

**(4) 프로바이더 직접 연결** — `91a799e` `a8cf40e`
- [x] airoute 프록시 제거 → `providers/registry.ts`에 9개 프리셋 직결(openai·gemini·grok·cursor·zai·minimax·opencode-zen/go·xai·openrouter·ollama)
- [x] 자격증명 자동 해석: 설정 → opencode `auth.json` → codex/gemini OAuth → macOS 키체인 → 환경변수
- [x] CLI 어댑터(`grok`, `cursor-agent`) — 네이티브 도구 호출이 없어 `caps.tools=false`로 강등, 도구 루프·검증 생략
- [x] 설정 화면 전면 재설계(프로바이더 카드 + 인증 상태 표시)

**(5) 봇 조직 통제** — `e0e7657` `a70ede8` `4d4b851` `cf48458` `448f358` `557ea71`
- [x] 팀장 하위 봇 한도(`max_children`, 기본 4) + 팀장의 봇 생성에 승인 게이트
- [x] 봇 이름 변경(`new_name`), 표시 순서 변경(`agent_reorder`), 계층 정렬(`parent_id` + `sort_order`)
- [x] 루프 차단: 봇 간 메시지 30분 10건, 위임 1시간 15건, 승인 재개 10분 3건
- [x] 삭제 시 고아 참조 정리(`deleteAgentRow`), 관리 도구 인자 별칭 정규화(배치 생성 지원)

**(6) 실행 수명을 HTTP 연결에서 분리** — `ab5820e` `5b06850`
- [x] `activeRuns` 제어기 — 화면 이탈·탭 닫기·탭 동결로 작업이 죽지 않고, 중단은 `POST /api/chat/stop`으로만
- [x] 명령 대기열 + 비동기 회신 실시간 표시("던져놓고 결과는 완료 순서대로")

**(7) 안정화·UI** — `7255ae5` `bf4b4c5` `ff0b10d` `184a6c5` `1e48d27` `957d5bb` `3fe3440` `96b48d2` `0a60ce9` `309fe55` `62caba6` `7235736` `23224c4` `8996c8f`
- [x] 3차에 걸친 전수 점검: 신규 설치 스키마, 디스패치 경쟁 조건, 비밀값 마스킹, 프롬프트 주입 방어
- [x] 빈/손상 응답 저장 차단, 검은 화면 크래시 수정, 라이트 에디토리얼 테마 전환
- [x] HTTPS 리스너(mkcert 인증서, :5443), 봇 프롬프트에 현재 시각(KST) 주입, 텔레그램 HTML 전송

### Phase 16 — 정리와 기반 정비 `[완료]` ✅ 2026-09-16
업무지침서 Phase 16. 실측 진단에서 드러난 데이터 위생·스키마 분산 문제를 정리했다.

- [x] R1 — 작업 디렉터리 루트 36개 항목을 6개로 정리: `briefing/`(9) · `morning-briefing/`(8) · `_archive/2026-09/`(16, 봇이 만든 실행 불가 스크래치)
- [x] R2 — 고아 봇 폴더 5개 정리. 살아 있는 봇과 짝이 맞는 3건(`IP 신청봇`→`IP신청봇`, `결제 처리봇`→`결재처리봇`, `메일 브리핑봇`→`메일분석봇`)은 MEMORY.md **끝**에 `[아카이브 병합]` 섹션으로 병합(앞 1500자만 프롬프트에 주입되므로 현재 맥락을 밀어내지 않게), 원본은 `_archive/2026-09/agents-orphan/`에 보존
- [x] R3 + C14 — 파일명 NFC 정규화. `safePath()`가 NFC로 정규화하고 NFC 후에도 남는 낱자모(U+1100~U+11FF, 모델이 만든 깨진 이름)를 제거하며, 기존 NFD 파일은 폴백 조회로 계속 읽는다
- [x] R4 — `parseLeaked`를 `server/src/toolloop.ts`로 추출. `chat.ts`/`team.ts` 복붙 2벌 → 1벌
- [x] R5 — `skills` 테이블 정의를 `routes/workspaces.ts` → `db.ts`로 이관(스키마 단일 소스). workspaces.ts는 기본 스킬 시드만 담당
- [x] C13 — `renameAgentFolder()` 신설. 이름 변경 시 폴더 이동 실패를 `catch {}`로 삼키지 않고 **응답 문자열과 서버 로그로 드러내며**, 대상 폴더가 이미 있으면 덮어쓰지 않고 병합한다
- [x] C16 — `server/src/maintenance.ts` 신설: 루틴당 실행이력 20건(그록봇 동일)·전체 90일, 처리된 승인/봇메시지 30일, 브라우저 캐시 월 1회. 진행 중(running/pending) 레코드는 제외하고, 브라우저 실행 중이면 캐시 정리를 건너뛴다
- [x] `agent_runs.routine_id` 추가 + 인덱스 — 루틴별 이력 보존·조회의 기준 키(업무지침서 A10의 전제)
- [x] tsconfig 수정 — `lib`에 `DOM.Iterable` 추가, 체크 대상을 `server/src`로 한정하고 `server/data` 제외
- [x] R8 — 이 절차서 현행화(Phase 15+ 미기록 구간 28커밋 반영)

> 검증 (2026-09-16)
> - 작업 디렉터리 루트: **36개 → 6개** (기준 20개 이하 통과)
> - `agents/` 폴더: 11개 → **6개**, 전부 DB에 실재하는 봇 (고아 0)
> - 파일 유실: **0건** (이동 전후 basename 전수 대조)
> - 새로 깨진 참조: **0건** (기존 미해결 참조 5건은 이동 이전부터 존재함을 백업 목록으로 확인)
> - 타입체크: **오류 15건 → 0건** (`tsc --noEmit` 통과)
> - 신규 DB 스키마: 18개 테이블 생성 성공, `skills.agent_id` 포함, 오류 **0건**
> - 보존 정리 드라이런: 현재 데이터 삭제 **0건**(전부 보존 기간 내), 루틴당 20건 상한 동작 확인(25건 → 20건)
> - 브라우저 캐시 정리 대상 **318MB/326MB**, 쿠키·로그인 자산과 경로 겹침 **0건**



### Phase 17 — 브라우저 조작 엔진 v2 `[구현 완료 · 실사용 검증 대기]` 2026-09-16
업무지침서 Phase 17. 실측에서 `browser_click` 실패율 50%, `browser_look` 70%였고 모델이 셀렉터 도구를 버리고 `browser_eval`로 112회 우회하던 상태를 고쳤다.

- [x] **참조 스냅샷** — 조작 가능한 요소에 `@번호`를 부여해 돌려준다. 번호는 DOM 속성(`data-mybot-ref`)으로 심어 다음 호출에서 정확히 재조회한다. 모델이 CSS 셀렉터를 추측할 필요가 사라졌다
- [x] **iframe 재귀 수집** — 본문이 iframe 안에 있으면 `[iframe 본문]`으로 덧붙인다. 그룹웨어 결재·메일 화면이 이 구조였고, 예전엔 메인 프레임만 읽어 "본문 미획득"으로 끝났다
- [x] **화면 상태 표기** — 모달 열림 / 로딩 중 / 화면 밖 요소(`·`) / 표시 상한 도달을 명시. 로딩 중 화면을 읽고 "목록 0건"으로 단정하던 오판을 막는다
- [x] **대기 전략 교체(`settle`)** — 고정 `waitForTimeout(1200~1500ms)`을 전부 제거하고 domcontentloaded + networkidle(상한 2초) + DOM 변화 정숙 대기(0.4초 무변화 / 2.5초 상한)로 대체
- [x] **수렴 폴링** — 화면이 미완이면 "더 이상 변하지 않을 때까지"만 기다린다(총예산 6초, 2회 연속 동일하면 중단). 고정 시간 재시도는 항상 조금 모자라거나 낭비였다
- [x] **실패 시 요소 목록 동봉** — 클릭·입력이 실패하면 원문 오류만 던지지 않고 지금 화면에서 고를 수 있는 요소를 함께 준다. 재시도가 1회로 끝난다
- [x] **팝업(새 창) 자동 추적** — 탭 스택 도입. `target=_blank`로 열린 창을 자동으로 따라가고 `browser_back`으로 목록에 복귀. 예전엔 "클릭은 됐는데 화면이 그대로"였다
- [x] **신규 도구** `browser_wait(text|selector|seconds)`, `browser_back`
- [x] **`browser_look` 재구현** — viewport만 JPEG(q60), 설정 `vision_model` 분리, 45초 상한, 실패 시 텍스트 스냅샷 폴백. 예전엔 풀페이지 PNG를 비전 지원 여부도 모르는 기본 모델에 태워 10회 중 7회가 120초 타임아웃으로 죽었다
- [ ] 실사용 검증(다우오피스 실제 시나리오) — 서버 재시작 후 사용자 환경에서 수행

> 검증 (2026-09-16, 격리 Chromium 실측)
> - browser.ts의 스냅샷 엔진을 **그대로 추출·트랜스파일**해 실제 Chromium에서 실행 (손으로 옮긴 사본 아님)
> - 실측 실패 로그를 본뜬 그룹웨어 픽스처(모달 · 지연 렌더 · iframe 목록 · `target=_blank`)에서 **15개 항목 전부 통과**
> - 노출 요소 수 **3개 → 11개** (구버전은 메인 프레임 링크만 보였다)
> - 실사이트(example / wikipedia / news.ycombinator) 스냅샷 생성 **10~19ms**, 오탐 0건
> - 테스트가 버그 3건을 적발해 수정: `offsetParent` 가시성 오판(`position:fixed` 모달), 렌더 완료 전 조기 읽기, 고정 재시도 시간 부족
> - 타입체크 오류 0


## 5. 운영 메모

- airoute 미기동 시 모델 호출 실패 → `~/.local/bin/airoute start` (이 맥에 이미 설치됨)
- Ollama(11434)/LM Studio(1234)는 현재 꺼져 있음 — 켜면 자동 감지
- 포트: **5274** 사용 (5273은 autoblog 점유 중)
- DB: `data/mybot.db` (SQLite WAL)

## 6. 진행 로그

| 날짜 | Phase | 내용 |
|---|---|---|
| 2026-09-14 | — | 조사(그록 기능/오픈소스 대안), 의사결정 확정, 절차서 v1 작성 |
| 2026-09-14 | 0 | 스캐폴드 완료. Bun+Hono+SQLite 서버(:5274), Vite+React+Tailwind 웹(:5275), 양쪽 타입체크·빌드 통과 |
| 2026-09-14 | 1 | 채팅 코어 완료. airoute 연동 210모델, SSE 스트리밍, 분기(편집/재생성/형제탐색), 자동 제목 |
| 2026-09-14 | 2 | Think 추론 패널 완료. reasoning_content/<think> 파싱, 실제 추론 저장 확인 |
| 2026-09-14 | 3 | DeepSearch 완료. DDG 차단 발견→Bing HTML로 교체, ck/a 디코딩 수정, 10페이지 정독+인용 리포트 검증 |
| 2026-09-14 | 4 | 이미지 생성(Images호환+DrawThings)+업로드 비전분석(data URL 주입)+텍스트파일 첨부 완료 |
| 2026-09-14 | 5 | 페르소나 5종+커스텀 CRUD+선택기, 메모리 자동추출/주입/관리, 접속암호 미들웨어 완료 |
| 2026-09-14 | 6 | 워크스페이스(필터+지침주입), /스킬(자동완성+서버확장), 루틴 스케줄러 완료. Bun idleTimeout 버그 수정 |
| 2026-09-14 | 7 | MCP stdio 클라이언트+도구 루프 구현 |
| 2026-09-14 | 8 | 음성: Web Speech API STT + speechSynthesis TTS (무설치 경로) |
| 2026-09-14 | 9 | launchd ai.mybot.server 등록, /tmp·~/Library/Logs 로그 경로 이슈 해결, PWA 아이콘 |
| 2026-09-15 | 1 | UI 수정: 좁은 창에서 입력줄 하단 칩 으깨짐→flex-wrap+shrink-0, 사이드바 하단 safe-area 패딩. 모델 피커 프로바이더/그룹 2단 구조로 개편(airoute 상세 모델 전부 선택 가능). Playwright 검증: glm-5.3-flash 선택→전송→airoute 라우팅 확인 |
| 2026-09-15 | 10 | 대장 봇(팀 오케스트레이션) 구현: team 모드 칩, agents/agent_runs 테이블, 계획→병렬 실행→종합 루프, 봇 도구(검색/파일/MCP), TeamTrace 패널, 설정에서 봇 관리. 실검증 완료 |
| 2026-09-15 | 10 | 봇 재사용 규칙 고도화: 대장이 기존 봇 명단 보고 페르소나 적합성 판단(부적합 시 신규 생성), 루틴 담당 봇은 [바쁨] 제외 + routines.agent_id 연결 + 루틴이 봇 역할·모델로 실행. 실검증 완료 |
| 2026-09-15 | 11 | 내장 브라우저(ego lite 증류): 영속 프로필 headed Chromium, 로그인 세션 공유, 탐지 신호 제거, 봇별 탭 격리, browser_* 도구를 팀봇+일반채팅에 연결, 설정에 로그인 창 열기. 실검증 완료 |
| 2026-09-15 | 11 | 브라우저 headless 전환: 봇 작업 new headless(channel:chromium), 로그인만 일시 headed. 실검증 완료 |
| 2026-09-15 | — | GitHub 공개: TechLLM/My-Personal-Bot, v1.0.0 릴리스, All Rights Reserved. 보안 점검+filter-repo로 data/ 히스토리 제거 후 force push. 영문 README 추가 |
| 2026-09-15 | 12 | 팀 계획 승인 흐름: 대장이 분해→계획 카드(봇별 체크박스+기존/새 봇 배지)→선택 실행(POST /api/team/run)/취소. 결과 알림: 설정에 텔레그램 봇+SMTP 메일 섹션(토글+테스트 발송), notify.ts, 일반/팀/루틴 결과 발송. 실검증 완료 |
| 2026-09-15 | 13 | 봇 중심 구조로 전환: 모든 대화가 봇에 귀속(conversations.agent_id, 기본=대장 봇 🧭 시드+기존 대화 백필), 봇 페르소나+도구로 모든 메시지 처리(일반 채팅에도 내장도구/브라우저/MCP), 봇별 장기기억(memories.agent_id) — 모델 바꿔도 봇 세션 맥락 유지. 실검증: 새 대화+다른 모델에서 이름 기억 확인 |
| 2026-09-15 | 13 | 장시간 실행 제한: 팀 봇 4분·일반 도구루프 3분·루틴 5분 타임아웃, 라운드 진행 이벤트, 도구 패널 "봇 작업 중" 표시 |
| 2026-09-15 | 14 | 루틴 봇 세션화: 봇이 routine_add/list/delete 도구로 대화 중 스스로 예약 등록(대장 페르소나에 지침). runRoutine을 runAgent(도구 루프)로 교체 — 봇 미지정 루틴은 대장 귀속, agent_runs 기록, 결과 대화 agent_id 귀속. 실검증: 대화로 루틴 등록·수동 실행 모두 봇 세션 통과 확인 |
| 2026-09-15 | 15 | 업그레이드 4종: (1) 사이트 계정 저장소(site_logins)+browser_login 도구 — 등록 계정으로 자동 로그인(그룹웨어 등), 비밀번호는 서버만 보유 (2) UI 이모지 전면 lucide 선형 아이콘 교체, 봇 아바타는 이름 해시 AgentIcon (3) 봇별 실제 모델명 표시 — airoute routes 맵 해석(subagent→devin/swe-2-max@max 등) (4) CEO 지정 — agents.is_boss, 설정에서 임의 봇을 CEO로 지정, CEO 전용 도구(agent_list/direct/update/delete), CEO는 팀 재사용 풀에서 제외·삭제 차단. 실검증: 로컬 로그인 폼 자동 로그인 성공, 대화에서 agent_list+routine_list 발동 확인 |
| 2026-09-16 | 15+ | 미기록 구간 28커밋 소급 정리: 협업 5종·검증 하네스·PGE 루프·프로바이더 직결·조직 통제·실행 수명 분리·안정화를 절차서에 반영 |
| 2026-09-16 | — | 코드 전수 분석 + 운영 DB 실측(실행 629건·승인 240건·도구호출 3,465건) + 그록봇 공식 문서 대조 → `업무지침서.md` 작성(Phase 16~23) |
| 2026-09-16 | 16 | 정리와 기반 정비: 작업 디렉터리 36→6항목, 고아 봇 폴더 5개 병합·아카이브, skills 스키마 db.ts 단일화, parseLeaked를 toolloop.ts로 추출, renameAgentFolder로 무음 실패 제거, safePath NFC 정규화, maintenance.ts 보존 정리 신설, agent_runs.routine_id 추가, tsconfig 정리(타입오류 15→0). 검증 통과 |
| 2026-09-16 | 17 | 브라우저 조작 엔진 v2: @번호 참조 스냅샷·iframe 재귀·모달/로딩 감지·수렴 대기·실패 시 요소목록 동봉·팝업 자동 추적·browser_wait/back 신설·browser_look 재구현. 격리 Chromium 실측 15/15 통과(버그 3건 적발·수정), 노출 요소 3→11개. 실사용 검증은 서버 재시작 후 |
| 2026-09-16 | 17+ | 메일 요약 실패 원인 분석: 서버 미재시작 + chat.ts 4라운드 제한 + 노트 재독 + 낡은 스킬. 도구 라운드 4→12, team.ts에 MEMORY.md 주입, 그룹웨어-메일조회 스킬을 @번호 절차로 교체(서버 API 경유), 그룹웨어 4개 봇 메모리에 정정 노트 추가 |
| 2026-09-18 | hotfix | 데스크톱 컴퓨터 사용(Computer Use) 도입: 그록봇 실조작으로 확인한 핵심 갭 메움 — computer.ts + COMPUTER_TOOLS 10종(look·apps·activate·click·doubleclick·rightclick·type·key·scroll·drag, JXA CGEvent+JPEG 비전 분석), 전량 승인 게이트. 실검증: 앱 전환·Cmd+N 창 생성·한글 입력·승인→실행 완주 |
| 2026-09-18 | hotfix | 추론 강도 실제 연결: chatOnce/streamChat/responses 계열에 reasoningEffort 옵션(openai→reasoning_effort, responses→reasoning:{effort}, 400 시 재시도). 보조 호출·도구 선택 라운드는 low, 최종 답변·종합은 기본 유지, 도구 사용 후 종료 시 보고서만 기본 추론 재작성. 실측 low 3.5s vs 기본 8.6s(2.5배) |
| 2026-09-18 | hotfix | 봇 간 연쇄 실행 폭주 차단 + 비서실장 폐지 + 속도 패키지: 사슬(chain) 순환 차단, 한 번도 작동 안 하던 핑퐁 가드 시각 비교 버그 수정, agent_update 승인 디듀프, 전체 중지 API+버튼, 429 쿨다운 120초, 중계 실행의 평가·보고서 재작성 생략, 답변 이중 생성 제거, CEO·Eggbot 도구 세트 축소(14,776→7,148자), secretary 역할 제거(CEO 직접 배정). 테스트 16건 통과 |
| 2026-09-18 | hotfix | 폴백 체인 데드엔드 수정: opencode-zen 잔액 부족(401 CreditsError)으로 zai 429 시 모든 폴백이 실패하던 것 → `minimax → zai → openai`로 교체, openai/gpt-6-astra 실측 응답 확인 |
| 2026-09-18 | evolve | 자기개선학습 S4 e2e: 실제 사이클로 discard→자동원복, 주입기준선으로 keep→승인행→실API승인→적용(applied=1) 전경로 검증. 벤치 발견 갭 3건 수정: file_exists 검사기 하위디렉터리 재귀화(오탐), bsk 디스패치 누락(26건 다우오피스 지시 축소 원인), 봇 실행에 memories 테이블 회상 미주입(G5 실패 원인→memoryCtx 추가). 기준선 33% 실측 — 자기개선 첫 개선 표적 확보 |
| 2026-09-18 | 24 | 자기개선 환경 분리: dev 워크트리(MyBot-dev·dev-env 브랜치)+launchd ai.mybot.dev(5275) 구성, MYBOT_ENV=dev 게이트로 사이클·틱은 dev 전용. keep → 승인팝업 대신 UpdatePackage를 서비스 POST /api/evolve/updates로 발송(실패 시 outbox), evolve_updates 원장+업데이트 센터(수신·적용·거부·되돌리기·버전·restart_required), 설정 업데이트 섹션+배지+SSE. 실API 검증: 서비스 사이클 거부·수신→적용→되돌리기→거부 전경로. dev 첫 사이클이 하네스 버그 3건 적발·수정(승인 위임사슬·승인기 상시화·잠금 행 finished_at) |
