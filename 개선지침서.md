# MyBot 개선 지침서

**점검일**: 2026-09-19
**점검 범위**: 조직 설계(`org_audit`) · 운영 실패 이력 · 자기개선 사이클 · 코드베이스·테스트 · 문서 정합성
**초기 점검 방법**: 규칙 기반 도구(`org_audit`) + 운영 DB 실측 + dev 인스턴스 이력 조회. 아래 기존 점검 수치는 당시 문서에 기록된 값이며 이번 진행 갱신에서 재측정하지 않았다.

## 진행 현황 — dev 개발·검증 → 서비스 업그레이드

**갱신일: 2026-09-19 · 사용자 확정 방향**

**E1은 완료된 과거 단계이고 서비스에는 미반영:** 당시 구현·독립 테스트는 `openai/gpt-6-astra / medium`, 설계·최종 리뷰는 `openai/gpt-6-astra / high`로 수행했다. 평가 장애·잘못된 응답·취소를 inconclusive로 처리하고 정상 0점으로 숨기지 않도록 dev에 반영했으며, 신규 관련 53개를 포함한 격리 스냅샷 전체 **119개 통과·0개 실패**와 서버 타입 검사·diff 검사를 기록했다. 실제 자기개선 벤치·서비스 업데이트·재시작은 하지 않았다. 이는 현재 작업자 정책이 아니라 역사적 기록이다.

**현재 요청 단계 — E2-A 오프라인 격리 기반 dev 반영·검증 완료, E2 전체는 부분 완료:** 총괄 설계·최종 리뷰와 제한된 E2-A 코드 게이트는 `gpt-6-astra / high`, 구현·테스트 작성은 소스→패치 API의 `gpt-5.6-sol`로 수행했고 반환 모델을 확인했다. `server/src/evolve.ts`, `server/src/evolve/{isolation,snapshot,protocol,worker}.ts`와 신규 테스트 2개, 총 7개 소스·테스트 파일을 dev에 반영했다. 기준선과 후보는 고정된 앱 소스·합성 초기화 레시피에서 각자 새 Bun 프로세스, 전용 DB·workspace·의존성 복사본으로 실행되며 코드·하네스·정책·실행 파일·의존성 해시와 실행 영수증을 남긴다. 후보에서만 달라지는 literal fixture로 실제 후보 코드 실행도 확인했다. 상세 범위와 한계는 [현재 작업 기록](tasks/todo.md)에 둔다.

검증은 하나의 외부 sandbox 실행으로 합쳐 주장하지 않는다. 일반 회귀+wire 테스트는 OS 네트워크·호스트 자격 증명 차단 아래 **171개 통과·0개 실패(656 assertions, 19 files)**, E2 OS 테스트는 통제된 테스트 부모 아래 실제 후보 worker마다 macOS `sandbox-exec`를 적용해 **23개 통과·0개 실패(209 assertions, 1 file)**였다. 합계는 두 suite의 **194개 통과·0개 실패(865 assertions, 20 files)**다. macOS가 중첩 sandbox를 거부하므로 E2 OS suite의 테스트 부모 자체가 외부 OS sandbox에 갇힌 것은 아니며, 부모 테스트 코드는 합성 소스·파일만 사용했다. 부모 서버 TypeScript 7.0.2 검사와 `git diff --check` 통과도 통합 근거로 확인됐다. 이번 단계에는 새 웹 변경·빌드가 없다.

이 결과는 **fixture 전용 E2-A 격리 구조**의 근거이지 성능·품질·비용 개선이나 실제 운영 상태 동등성의 증거가 아니다. MyBot 업무 벤치용 실모델·실계정·실제 골든 업무를 실행하지 않았고 서비스·프로세스·release·커밋·외부 Telegram/메일을 변경하지 않았다. 현재 소스의 자동 주기는 후보 제안·materialize·계측 전에 보류되며, production runner 결과도 점수와 무관하게 승격 불가다. 이 소스가 활성화되면 **E2-B 안전한 실모델/골든 연결 전까지 자동 제안·측정·발송을 코드로 보류**한다. 실행 중 프로세스를 실제로 중지시켰다는 뜻은 아니다. 실행 중 dev의 버전이나 자동 reload 여부는 확인하지 않았다. keep→수령→적용 왕복 실측과 승인 과제 샌드박스 정책(G7)은 dev에서 실측 완료했다 — 실제 영수증으로 검증·수령·적용·되돌림이 통과했고, 승인 과제는 시드 충실도·lifecycle 체크 수정 후 샌드박스 자동 승인으로 측정된다. 다음 우선순위는 **추가 반복 실측 → 자동 사이클 재개 검토**이다. 브라우저·MCP 샌드박스 정책(읽기 전용 브라우저+선언 MCP, 실모델 실측 완료)과 비용 비교 기준(토큰 축 판정·토너먼트 순위)은 도입됐다. 대량 실측 1회로 eval_min의 toolLog 미전달 버그를, G7 실측으로 시드 역할 누락·lifecycle 허점을 발견·수정했다. E2-B(브로커·골든 브리지·도구 대행)는 완료, E3(holdout 게이트)·E4(팔별 대칭 예산·비용 기록)·E7(서비스 영수증 검증·인증 발송)은 도입됐다.

- 개발과 검증은 `MyBot-dev`에서 수행한다. 서비스 공간 `MyBot`은 개발 작업장으로 사용하지 않는다.
- dev에서 검증한 변경을 버전별 릴리스로 준비하고, 서비스에서는 사용자가 확인한 버전을 업그레이드한다.
- 한 단계씩 진행한다. 개발 완료, 서비스 전달 준비, 서비스 수령, 사용자 승인, 적용, 실제 실행 검증을 각각 기록한다. 테스트 통과나 파일 저장만으로 서비스 업그레이드 완료라고 쓰지 않는다.
- 서비스 코드·DB·설정·웹 빌드·프로세스는 업그레이드 단계의 별도 확인 전에는 변경하지 않는다. 이번 갱신에서 배포·재시작·암호 생성은 하지 않았다.
- 최신 진행 현황은 **dev의 이 문서**, 1단계 상세 근거는 [접근 통제 작업 기록](tasks/access-stage1.md)에 둔다. 서비스 쪽 문서는 릴리스에 포함해 함께 승격할 대상이며 이번에는 수정하지 않는다.

### 단계별 상태

| 단계 | dev 작업 상태 | 검증 근거 | 서비스 상태 | 다음 완료 조건 |
|---|---|---|---|---|
| 1. 접근 통제 | 소스 적용됨. 현재 dev 인증 API 503(암호 미설정) | 이전 9개 테스트·타입 검사·격리 빌드 기록, 이번 GET 재확인 | **서비스 코드 반영·미인증 401 확인**. 전체 인수 검증은 별도 | 서비스의 후속 수정과 dev 변경 대조, dev 암호·실제 접속 검증, 최종 실행 버전 확인 |
| 2. 개발·운영 권한 분리 | 격리 작업트리에 초안만 있음. dev 정식 반영 전 | 통합 초안 테스트 기록은 있으나 단계별 인수 검증 전 | 미전달·미적용 | 운영 계정·키·프로필과의 분리 및 dev 외부 영향 차단 검증 |
| 3. 격리된 자체 개선 검증 | **E1 + E2-A 완료, E2-B 완료(골든 브리지 + 도구 브로커 + 실모델 실측), E3 holdout 게이트·E4 팔별 예산·E7 영수증 검증 도입, 승인 과제(G7) 샌드박스 정책 도입**. 격리 worker가 브로커 경유 실모델로 실제 파이프라인을 실행하고 팔별 계측을 남긴다 | 전체 303 pass. 대량 실측 1회: 24회 실모델 실행 — eval_min toolLog 버그 수정 후 23/24. G7 실측: 시드 역할 누락·lifecycle 허점 수정 후 승인 과제 양팔 실측 통과. keep→수령→적용→되돌림 왕복 실측 통과 | 미전달·미적용 | E7은 구조 검증만 — 영수증의 진위는 dev 격리 인프라가 보장. 샌드박스 자동 승인은 실제 승인 팝업 검증이 아님. 남은 것: 추가 반복 실측, 자동 사이클 재개 |
| 4. 승인·배포·복구 | 안전 보강 초안 별도. dev에는 새 release.ts 미반영 | 이번 release API GET 확인. 적용·재시작·복구 시험은 하지 않음 | **관리자 release 통로 추가·조회 응답 확인**, 안전 보강은 미완료 | 승인 SHA·검증 영수증·적용 전 저널·실제 복구 검증 |
| 5. 봇·예약 작업 수명주기 | 삭제 가드·예약 큐·상태 구분 초안만 있음. dev 정식 반영 전 | 단계별 통합·실기기 검증 전 | 미전달·미적용 | 진행 작업·예약·승인의 종료·보존·취소 규칙 검증 |

2~5단계 안전 보강 초안은 `codex/mybot-safety-boundaries`에 있으며 그 작업트리에서 기록한 80개 테스트 결과는 **현재 dev나 서비스 전체의 검증 결과가 아니다**. 이후 별도 추가된 서비스 release 통로와도 구분한다. 초안을 한꺼번에 반영하지 않는다.

### 2026-09-19 재점검: 워크플로와 근거 기반 업데이트

**결론:** E2-A는 dev에서 기준선·후보를 오프라인 fixture로 독립 실행하고 실행 코드를 식별하는 기반을 갖췄다. E2-B로 격리 worker가 브로커 경유 실모델 + 실제 파이프라인으로 골든 과제를 실행해 개선 효과를 측정할 수 있게 됐다. 다만 외부 도구·승인 범위 정책과 실제 외부 모델 실측은 남아 있고, 자동 사이클은 E3 동일 조건·E4 비용 계측까지 보류된다. 서비스 반영은 별도 관리자 승인 구조이며, E7로 서비스가 패키지의 격리 영수증·측정 완전성·ops-측정 일치를 수령·적용 시 독립 검증하게 됐다. 따라서 **모델 종류와 관계없이 검증된 개선만 업데이트한다는 구조는 갖춰졌으나, 실제 keep→발송→수령→적용의 실측 왕복은 아직 없다.** 배포 가능한 상태, 격리 기반이 검증된 상태, 실제 개선이 입증된 상태를 구분한다.

### 2026-09-20: 역할별 모델 배정 + E2-B 자격 증명 broker 착수

**역할별 모델 배정(`evolveModel`)** — 자기개선 루프의 모델 호출을 역할로 나눠 필요한 크기만 쓰게 했다:

| 역할 | 배정 | 근거 |
|---|---|---|
| 탐색·제안 (explore) | Eggbot 설정 모델 | 플래너 — 실패 분석·후보 발굴은 강한 모델이 필요 |
| JSON 추출 (extract) | 기본 모델 + 낮은 추론 | 보고서 → JSON 기계 변환 |
| db 값 재작성 (rewrite) | 기본 모델 + 낮은 추론 | 한정된 변환 |
| 코드 재작성 (codegen) | **작업급 모델(Eggbot)** | 파일 전체 재작성 — 약한 생성은 벤치 판을 통째로 날린다 |
| 골든 벤치 | 측정 대상의 운영 모델 그대로 | 싼 모델로 재면 측정이 무효 — 절약 대상 아님 |
| 품질 평가 | 기본 모델 | 생성자와 다른 모델 — 자기확증 방지 |
| 의도 분류 | 빠른 모델, 실패 시 작업 모델로 에스컬레이션 | 이미 적용됨 |

- `internal: true` 실행 옵션 추가 — 자기개선 탐색처럼 결과를 기계가 소비하는 내부 실행은 최종 보고서 재작성·품질 평가를 건너뛴다 (사이클당 작업 모델 호출 2~3회 절감).
- 설정 `evolve_models`(JSON)로 역할별 모델을 재지정 가능 — 운영자가 코드 변경 없이 조정한다.

**E2-B 자격 증명 broker(`evolve/broker.ts`) 착수** — 샌드박스는 `deny network*`라 worker가 네트워크를 못 쓰므로, 부모가 루프백 포트에 브로커를 열고 샌드박스 정책이 그 포트로의 outbound만 허용한다. worker는 `ctx.chat`으로 토큰 인증 호출만 하고 API 키·실제 endpoint는 부모에만 있다. 브로커가 모델 허용목록·호출 수·토큰 상한을 집행하고 호출을 계측한다(E4 지점). 샌드박스 내 왕복·허용목록 차단·타 포트 차단을 fixture 테스트로 검증했다.

**E2-B 골든 브리지 완료** — `mode:"production"`이 실제로 동작한다. 격리 worker는 브로커를 OpenAI 호환 프로바이더(`broker/<bare>`)로 등록해 `runAgentDetached` 파이프라인을 무수정으로 재사용하고, 시드된 봇의 모델만 broker 경유로 재배선한다. 샌드박스 안에서 모델→도구→모델 왕복과 골든 체크 평가까지 실측으로 검증했다. 팔별 계측은 토큰의 `:arm` 태그로 분리되고 부모가 `GET /usage`로 읽어 영수증 `model.{requested,resolved,executed,calls}`에 기록한다 — 파이프라인 호출은 worker가 못 보지만 브로커는 전부 본다. 실패 시 영수증 `stderrTail`이 샌드박스 안 진단을 남긴다. `runCycle`은 이제 골든 과제마다 격리 비교를 돌려 `judge`로 판정하고 원장을 마감한다(승격 불가 불변).

**도구 브로커 + E3 holdout + E4 대칭 예산 (같은 날 후속)** — 브로커에 `/tool` 경로를 추가해 샌드박스 안에서도 허용된 외부 도구를 부모가 대행한다. `web_search`만 명시 허용하면 G6 같은 검색 과제도 네트워크 차단 상태에서 측정된다(허용 목록 밖 도구는 403 → 체크 실패로 정직하게 반영). E3: 주 세트에서 `keep`이 나와도 holdout 과제(H1–H3)를 같은 격리 비교로 한 번 더 재고, holdout 통과율이 기준선보다 낮으면 `discard`(주 세트 과적합 차단). E4: `maxCallsPerTag`·`maxToolCallsPerTag`로 팔별 예산을 같게 집행하고, 원장에 `brokerUsage`(팔별 호출 수·추정 토큰·실제 모델)를 기록하며 판정 이유에 토큰 추정을 붙인다. **실제 모델 실측**: dev의 `zai/glm-5.3-flash` 키로 진짜 격리 골든 실행을 완주했다 — 양 팔 각 2회 호출·~1.8k 토큰, `agent_list` 도구와 봇 수(7개) 응답이 실제로 일치. **남은 한계**: `openai/gpt-6-astra`는 dev에 키가 없어 그 모델을 쓰는 봇(CEO·Eggbot)은 dev에서 실측 불가 — glm을 쓰는 봇으로 대상을 바꿔 실측했다. 승인 필요 과제는 `approvals:"auto"` 선언분만 벤치 포함. 브라우저는 읽기 전용 5종·MCP는 과제 선언분만 브로커 대행(아래 샌드박스 도구 정책). 자동 사이클은 보류 유지(수동 `/evolve/cycle`만 가능).

**릴리스 적용 실패 사고와 스테이징 검증 (같은 날)** — `ec04fb0` 대기 묶음의 서비스 적용이 테스트 게이트에서 실패해 자동 롤백됐다. 원인 분석: merge로 서비스 트리를 새 코드로 바꾼 뒤, 실행 중인 서비스와 외장 RAID 위에서 무거운 스위트(270개)를 돌리는 구조가 문제였다. 같은 스위트가 빠른 로컬 디스크(`/tmp`)에서는 4.9초인데 적용 시엔 99.9초 걸렸고, 샌드박스 자식을 띄우는 E2-A 테스트들이 팔별 `node_modules` 복사와 5초 기한을 견디지 못해 실패했다 — 코드 결함이 아니라 **검증 환경의 일시적 부하가 좋은 업데이트를 떨군 것**. 개선:

- `applyRelease`를 2단계로 변경 — ① 대상 커밋을 `git worktree`로 임시 디렉터리(빠른 로컬 디스크)에 체크아웃하고 `bun install` 후 전체 게이트(테스트·tsc·웹 빌드·기동 시험)를 돌림 → ② 통과한 트리만 `merge --ff-only`, 의존성 동기화(`bun install` — 릴리스가 패키지를 바꾼 경우의 잠재 결함도 해소), 웹 빌드 + 기동 시험만 재확인.
- 스테이징 실패는 새 영수증 `rejected`로 기록 — 서비스 코드가 한 줄도 안 바뀐 실패와 롤백을 구분하고, 실패 문구에 "코드는 바뀌지 않았다"를 명시. 영수증 진단 보존은 400→800자.
- 격리 기한은 정지 감지용 — fixture 기본 기한 5초→30초, 서브프로세스 계열 테스트의 bun 타임아웃 10~15초→60초(판정은 그대로).
- `release_inflight` 저널로 적용 중복 실행 차단. 윈백·revert 경로에도 `bun install` 동기화 추가 — 되돌린 코드와 node_modules가 어긋나는 같은 부류의 구멍.
- 경고: 이번 개선이 **적용되기 전의** 다음 적용은 여전히 구 흐름(merge 후 무거운 스위트)으로 돈다 — 릴리스 채널에 이 커밋이 들어간 뒤의 적용부터 스위트가 완화된 기한으로 돌지만, 스테이징 자체는 그다음 적용부터 유효하다.

#### 새 워크플로는 기존 합의와 같은가?

같다. 새 Claude 규칙은 `dev 개발·검증 → release 브랜치 → 서비스 설정 화면에서 관리자 적용 → 검증·재시작`이다. 규칙 원문은 저장소의 AGENTS.md가 아니라 `/Users/noah/.claude/projects/-Volumes-Acasis-Raid-noah-offload-archives-MyPJ-MyBot/memory/mybot-release-workflow.md` 11–21행에서 확인했다.

| 경로 | 대상 | 사람이 확인하는 지점 | 현재 구현 |
|---|---|---|---|
| 관리자 서비스 릴리스 | 사람이 검토한 앱 전체 코드·신규 파일·웹 빌드 | 서비스 설정 → 서비스 릴리스 적용 | [서비스 release.ts](../MyBot/server/src/release.ts) 63–114행. release를 fast-forward 병합한 뒤 테스트·타입검사·웹 빌드, 성공하면 재시작 예약 |
| 봇 자기개선 업데이트 | 등록 표면의 봇·스킬·루틴·기존 코드 수정 | 서비스 설정 → 자기개선 업데이트 적용 | [evolve.ts](server/src/evolve.ts) 376–418, 675–719행. dev의 keep 후보를 서비스에 보관한 뒤 별도 적용 |
| 실행 중 봇의 도구 승인 | 개별 외부 영향 작업 | 도구 승인 팝업 | 위 두 업데이트 경로와 별개. 이를 통과했다고 코드 개선·배포 검증이 된 것은 아님 |

`release.ts`는 **로컬 `refs/heads/release`**를 읽는다(35–41행). 두 공간은 Git 저장소를 공유하므로 이 참조가 배포 채널이며, 코드에 원격 fetch/pull은 없다. “release로 민다”는 표현만으로 원격 push를 하면 서비스가 자동 수신한다고 해석하지 않는다. 봇의 protected 목록은 풀지 않는다.

#### 이전 읽기 전용 점검 시점의 상태 (E1 개발 전)

- 서비스 HEAD: `8192da1963c53564450e1cb6ebb22b26fde6e595`. 접근 통제와 관리자 릴리스 코드가 들어 있다. 미인증 `GET /api/access`는 **401**, 인증된 `GET /api/release`는 **200**으로 현재 경로가 응답함을 확인했다.
- 조회 당시 릴리스 대기 **1건**, `canApply=true`, `appliedAt=0`이었다. **이 값은 브랜치·청결·fast-forward 조건일 뿐, 대기 변경의 개선 효과나 안전성을 검증했다는 뜻이 아니다.** 버튼을 누르지 않았다.
- dev HEAD는 `9c49370b35878a6454942ad5515253c919bd3e0b`이며 미커밋 변경이 있다. dev에는 `release.ts`가 아직 없다. 소스·문서 동기화 전 변경을 보존하고 충돌을 검토해야 한다. 이번에 병합·재배치하지 않았다.
- dev의 미인증 `GET /api/access`는 **503**이다. DB 접속 암호가 비어 있고 키 파일도 없다. 관리자 API 잠금은 자기개선 스케줄러 중단을 뜻하지 않는다.
- launchd 설정은 dev만 `MYBOT_ENV=dev`이며 두 인스턴스 모두 `--hot` 없이 실행한다. dev 코드의 `startEvolveLoop()` 호출은 남아 있지만, 현재 소스의 daily tick은 모델 제안·materialize·계측 전에 보류된다. 실행 중 프로세스를 재시작하거나 버전을 계측하지 않았으므로 현재 프로세스가 이 동작을 사용한다고 단정하지 않는다.
- 실험 원장 조회: dev **keep 1 / discard 2**, 서비스 **discard 1**. 서비스의 evolve 업데이트 원장은 **rejected 2 / reverted 1**, 현재 applied 항목은 없다.
- 이전 문서의 “릴리스 통로가 없다”, “서비스에 1단계가 미반영”, “양쪽 모두 암호 미설정”은 이후 다른 작업의 반영으로 더 이상 현재 상태를 설명하지 못한다. 이번 점검은 코드·설정·읽기 전용 DB 조회·GET 확인만 했고 배포·재시작·모델 호출·벤치·테스트는 실행하지 않았다.

#### 모델과 무관한 근거 게이트: 확인된 결함

읽기 전용 점검 당시 두 공간의 `evolve.ts`·`evaluate.ts`는 동일했다. 이후 E1과 E2-A를 dev에만 반영해 현재는 다르다. 아래 E2~E8의 소스 위치와 결함 설명은 **E1 이전 역사적 점검 스냅샷** 기준이며 release 관련 항목은 당시 서비스 소스 기준이다. 현재 해결 여부는 각 행에 별도로 적는다.

`judge()`에는 특정 모델 이름을 우대하는 조건이 없다. 그러나 입력 점수 일부는 기본 모델의 채점 결과이므로 **분기 규칙은 모델 이름과 무관해도 최종 판정은 채점 모델에 의존한다**. 이번에는 새 후보를 실행하지 않았으므로 특정 과거 실험이 반드시 기존 코드로만 실행됐다고 단정하지 않고, 실제 후보 실행을 증명하는 경계가 없다는 결함으로 기록한다.

| ID·우선순위 | 근거와 발생 조건 | 영향 | 개선 방향 |
|---|---|---|---|
| E1 · P0 · dev 완료 | [evaluate.ts](server/src/evaluate.ts), [evaluation-evidence.ts](server/src/evaluation-evidence.ts), evolve.ts의 checkTask/runBench/judge/runCycle | 기존 제공자 실패의 pass=true·100점을 fixture로 재현한 뒤 차단. 서비스에는 아직 남음 | inconclusive 전파·오류 기준선 조기 중단·빈 검사/비유한 수치 차단. 단위·회귀 검증 완료, 운영 검증 미수행 |
| E2 · P0 · **E2-A 완료·E2-B 골든 브리지 완료** | **역사적 위치** `evolve.ts:278–303, 394–403`: 실행 중 폴더·DB에 후보를 적용하고 같은 프로세스에서 벤치 실행했던 경로 | live apply/revert 및 같은 프로세스 비교 문제는 E2-A에서 닫힘. E2-B로 격리 worker가 브로커 경유 실모델 + 실제 파이프라인(runAgentDetached)으로 골든 과제를 실행 — 개선 효과를 측정할 수 있게 됨 | E2-A: 독립 프로세스·합성 초기화·영수증 완료. E2-B: 브로커·골든 브리지·팔별 계측 완료. 남음: 외부 도구·승인 범위 정책, 실제 외부 모델 실측 |
| E3 · P1 · **부분 완료** | holdout 기본 제외, 전체 표본 수·평균 통과율·평균 지연만 비교 | 특정 과제 퇴행이 가려질 수 있음 | **도입됨**: 주 세트 keep 시 holdout(H1–H3)을 같은 격리 비교로 재측정, holdout 하락 시 discard. 남음: 과제별 반복 표본(현재 과제당 1회), 서비스 측 holdout 보관 |
| E4 · P1 · **부분 완료** | 토큰 0 고정, BenchResult에 비용 없음. judge에 비용·안전 위반 수 검사 없음 | “비용 비열등·안전 위반 0” 계약을 입증하지 못함 | **도입됨**: 브로커 팔별 호출·추정 토큰 계측, `maxCallsPerTag`/`maxToolCallsPerTag` 대칭 예산, 원장·판정 이유에 비용 기록. 남음: 실제 가격표 기반 비용 비열등 판정, 안전 위반 수 불변식 |
| E5 · P1 | `evaluate.ts:32–45`: 채점자가 받는 근거는 도구 이름과 보고문. `evolve.ts:95–112`: 생성 흔적·호출 이름 위주 검사, toolLog.ok 미확인 | 도구 호출·보고서 존재가 업무 성공으로 오인될 수 있음 | 실제 DB 변화·이번 실행의 파일 해시·도구 성공·외부 영수증으로 판정. LLM 평가는 보조로만 사용 |
| E6 · P0 | **역사적 위치** `evolve.ts:170–176, 202–216`: 벤치 시작 이후 모든 pending 승인을 자동 승인하고 타임아웃은 대기만 종료 | 해당 legacy `autoResolve` 실행 경로는 E2-A에서 비활성화됐지만, 실제 도구를 위한 run/action 단위 승인·자격 증명·취소 정책은 아직 없음 | E2-B에서 벤치 전용 계정·DB·외부 도구 broker, 승인 범위 및 실제 취소를 설계·검증 |
| E7 · P1 · **dev 완료** | `evolve.ts` `verifyUpdatePackage`: 수신·적용 때 패키지의 측정 증거를 재검증 | ~~관리자 승인만 있으면 미검증·오래된 패키지도 적용 가능~~ → 차단됨 | **도입됨**: 패키지에 `evidence`(실험 id·팔별 격리 영수증·holdout·브로커 계측·candidateKey) 결속, 수령·적용 시 재검증. 거부는 rejected로 기록해 사유 표시. 남음: 영수증의 암호학적 출처 증명 없음 — 서비스는 구조 정합성만 검증하고 실측 진위는 dev 격리 인프라에 의존 |
| E8 · P1 · **dev 완료** | dev의 발송에 인증 헤더가 없었음 | ~~정상 후보라도 인증된 서비스 전달에 실패~~ → 해결됨 | **도입됨**: `serviceKey()`가 `MYBOT_SERVICE_KEY` → 키 파일 → 서비스 DB `access_code` 순으로 읽어 `x-mybot-key`로 발송. 실패 시 outbox 보관. 남음: 실제 keep→수령 왕복 실측 |
| R1 · P1 | `release.ts:26–32, 63–82`: 적용 가능 여부는 Git 조건만 확인. 서비스 작업 트리에 먼저 병합한 뒤 테스트·빌드 | “검증된 것을 활성화”가 아니라 “서비스 파일을 바꾼 뒤 검증”. 테스트·빌드 성공도 업무 개선 효과를 증명하지 못함 | 별도 후보 체크아웃에서 검증한 산출물을 승인된 SHA로 활성화. dev 검증 영수증·업무 회귀 검사 필요 |
| R2 · P1 | `release.ts:63–88, 105–114`: 승인 대상 SHA 입력 없음, 되돌릴 SHA는 성공 후 저장, 재시작 후 건강 검사 없음. rollback의 reset·빌드 결과도 확인하지 않음 | 클릭 전후 채널 변경·중간 종료·복구 실패·새 프로세스 실패를 확정적으로 처리하지 못함 | 승인 SHA 고정, 작업 drain·적용 전 영속 저널, 각 복구 단계 결과 검사, 새 실행 버전·health 검증 |
| R3 · P1 | `release.ts:77`: server 디렉터리에서 `bun test`를 넓게 실행. `release.test.ts`는 조건 판정 함수 위주 | 로컬 작업 파일을 테스트로 수집할 위험과 실제 배포·복구 검증 공백 | 테스트 경로를 명시하고 네트워크·계정·파일을 격리, 실패 주입·재시작·복구 통합 테스트 |

**keep 기록 재평가:** dev의 keep 1건은 기준선 `passRate=0, avgLatencyMs=999999, samples=3, byTask 없음`, 후보 `passRate=0.3, avgLatencyMs=44520, samples=30, byTask 10개`다. 둘 다 holdout·비용·코드 식별 근거가 없다. 값의 생성 의도를 단정할 수는 없지만 **동일 조건의 실측 개선 증거로 인정할 수 없으며 재측정이 필요하다.** 이 기록을 지우거나 상태를 바꾸지는 않았다.

역사적 `mandela` 점검에서는 보고문을 LLM이 다시 채점하는 **Shared hallucination** 위험과, 후보 적용 후 기존 런타임을 계속 쓰는 **Wrong null hypothesis** 위험이 확인됐다. 후자는 E2-A의 독립 worker와 후보 전용 literal probe로 차단했지만, 전자는 실제 골든 완료 조건을 연결할 E2-B/E5 과제로 남는다. 숫자로 나온 점수 자체가 독립적인 사실 검증은 아니다.

#### 개선 지침서를 진행해야 하는가?

**진행해야 한다.** 자동 루프는 최근 실행 오류·거부된 승인·저성공 스킬을 읽어 후보를 제안한다(`evolve.ts:518–561, 630–641`). 이 개선 지침서의 항목을 작업 큐로 읽어 이행·완료 표시하는 연결은 확인되지 않았다. 문서는 사람과 개발 에이전트의 실행 백로그이며, 자기개선이 알아서 모두 처리한다고 기다리면 안 된다.

사용자 요청의 **최종 결과 1회 전달·사람용 승인 문구**와 **E2-A 오프라인 독립 실행 기반**은 dev 소스 구현·격리 검증을 마쳤다. 다음에는 E2-B 실제 골든/모델 브리지와 E3 동일 조건 비교를 보강하고 서비스 후속 변경과 dev 기준을 정합시킨다. 새 release 통로를 중복 구현하거나 보호 경로를 해제하지 않는다. 모델 확대·예산 증액·사이클 빈도 증가는 이 안전 조건을 갖춘 뒤 검토한다. 기존 순서의 번호는 추적용으로 유지하되, 완료 기준을 충족하지 못한 단계는 완료 처리하지 않는다.

모델 중립적인 목표 계약은 다음과 같다. **어떤 모델도 후보를 제안할 수 있지만, 모델의 자신감·설명·자기 채점만으로 업데이트 권한을 얻지 않는다.** 후보는 같은 완료 기준 아래 독립 실행 증거, 과제별 회귀·holdout, 비용·안전 검사, 정확한 코드/데이터 해시를 갖춰야 하고 검증 불가이면 보류한다. 관리자는 그 동일한 후보를 승인하며, 배포 후 새 버전의 건강 검사까지 성공해야 서비스 업데이트 완료다. “모든 모델의 결과 품질이 같아진다”는 보장은 아니다.

### 2026-09-20 (후속): 승인 과제(G7) 샌드박스 정책 + 시드 충실도 수정

**승인 과제의 샌드박스 정책 도입** — 골든 과제가 `approvals:"auto"`를 선언하면 격리 worker가 **그 팔의 합성 DB에만** `^agent_` allow 규칙을 심어 승인 게이트를 우회한다. 측정 질문은 "승인이 주어졌을 때 봇이 수명주기 절차를 올바르게 수행하는가"이며, 실제 사용자 승인 팝업·재개 경로는 이것으로 검증되지 않는다. 서비스 승인 코드(`approvals.ts`)는 미수정 — 샌드박스 일회용 DB 안에서만 유효한 정책이다. `benchTasks`는 `auto` 과제만 포함하고 다른 승인 과제는 계속 제외하며, holdout은 `!t.approvals`로 더 엄격하게 유지한다.

**실측이 찾아낸 결함 3개:**

- **시드가 조직 역할을 버림(측정 무효 결함)** — 시드 INSERT가 `id·name·role_prompt·model`만 써 `is_boss·is_lead·special_role·parent_id` 등을 버렸다. 샌드박스의 CEO·Eggbot이 `MANAGE_TOOLS` 비노출로 생성·삭제 도구 자체가 없었고, CEO도 일반 봇 프롬프트로 강등돼 **모든 과제가 운영과 다른 봇 구성에서 측정**되고 있었다. `SeedAgent`·worker INSERT·`liveAgents()`를 고쳐 조직 구조를 그대로 운반한다.
- **lifecycle 체크 허점(부정확한 pass)** — `agent_runs.task LIKE %name%`이 부모 실행 프롬프트와 항상 매칭돼 "생성 흔적 있음"이 사실상 항상 참이었다(생성 시도 없이 보고만 한 팔이 pass로 재현 확인). 실제 **실행** 증거만 인정하게 수정 — 어느 실행의 `tool_log`에서 `agent_create ok:true`(위임 포함), 또는 승인 경로로 실행(`status='approved'`)까지 간 생성 요청. `evolve.ts`의 레거시 `checkLifecycle`도 같은 허점이라 같이 고쳤다.
- **G7 과제 정의 결함** — 대상 봇 이름 `골든테스트봇`이 dev DB에 이미 존재 → 시드에 포함돼 `agent_create`가 UNIQUE 충돌로 결정적 실패. 이름을 `골든수명주기봇`으로 교체했다.

**실측 결과** — 수정 후 실모델 격리 측정에서 양팔 모두 엄격한 증거로 pass: 생성 실행 증거 + 최종 부재. CEO→Eggbot(org_admin) 위임→`agent_create`→상태 확인 지시→`agent_delete` 전체 체인이 샌드박스 안에서 실제 완주했다. 브로커 계측으로 팔별 호출·모델이 기록된다(위임 봇의 실제 모델 포함). 회귀: 전체 **303 pass / 0 fail**, lifecycle 경계 테스트 5개 추가, tsc 0.

**남은 한계** — 승인 과제의 팔별 편차가 커서(모델이 위임·거부·직접 수행 중 택함) 반복 측정 안정성은 추가 관찰이 필요하다. (브라우저·MCP 정책은 아래 항목에서 도입 — 읽기 전용 브라우저·선언 MCP)

**테스트 순서 오염 사고 (같은 날)** — `2a4d773` 묶음의 서비스 적용이 스테이징 검증에서 `rejected`됐다(서비스 무결 — 새 흐름이 설계대로 동작). 실패는 브로커·격리 테스트 12개로, 모두 기대 401/403에 **400**이 온 것이 특징이었다. 원인: `org.test.ts`가 모듈 최상위에서 `globalThis.fetch`를 전부 400을 반환하는 mock으로 **영구 교체하고 복원하지 않았다**. 테스트 파일 실행 순서는 디렉터리의 물리적 순서(readdir)에 따라 달라지며, dev에서는 org가 브로커보다 뒤에 와 우연히 통과했지만 새 worktree 체크아웃에서는 앞에 와서 뒤의 모든 실제 fetch가 mock에 걸렸다. 수정: mock을 `beforeAll`/`afterAll`로 이 파일의 테스트 구간에만 한정. 다른 mock 파일들(command-delivery·notify·command-integration·web-access·isolation-integration)은 전부 복원 구조가 있음을 확인했다. 교훈: **`globalThis`를 바꾸는 테스트는 반드시 복원 경계를 둬야 하며, 통과가 파일 순서에 우연히 의존하면 안 된다.**

**inflight 마커 잔류 사고 (같은 날)** — 위 `rejected` 적용 이후 모든 적용이 "이전 적용이 아직 끝나지 않았습니다"로 차단됐다. 원인: `applyRelease`의 `reject()` 경로가 `release_inflight` 저널을 지우지 않았다. 거부는 종결 상태인데 inflight가 남아 영구 잠금이 됐다. 다른 모든 종료 경로(worktree 실패·병합 실패·롤백·성공)는 `clearJournal()`을 불렀고 거부만 빠져 있었다. 수정: `reject()`가 `clearJournal()`을 호출. 회수: 서비스 재시작 시 `recoverJournal()`이 기동 중에 끊긴 inflight를 영수증으로 남기고 지우는 정상 경로로 해소. 교훈: **inflight·락·저널을 세우는 코드는 모든 종료 경로(성공·실패·거부·예외)가 해제를 보장해야 하며, 경로를 추가할 때 해제 누락을 전수 확인한다.**

**다방법 경쟁(토너먼트) 추가** — 소유자 지시: "하나의 결과물을 만들기 위해 다양한 방법을 시도하고, 올바른 결과 > 속도 > 토큰 순으로 최선을 찾아 적용해야 한다." 점검 결과 기존 루프는 후보 1건만 제안·측정했고 토큰은 계측만 되고 판정에 쓰이지 않았다(계약 §4의 "유료 호출 비용" 축이 미구현). 구현: ① `proposeCandidates`가 같은 목표의 서로 다른 접근(지시문·도구 구성·모델·스킬 등)을 최대 variantsPerCycle(기본 4, 최대 10)개 제안 ② `runTournament`가 후보별 쌍대 격리 비교를 돌리고 팔 태그 접미사(`-0`,`-1`…)로 후보별 토큰을 분리 계측 ③ 완전한 측정(평가 게이트 통과) 중 통과율→지연→토큰 사전식 순위로 승자 선택, 승자만 holdout·발송 ④ `judge`에 토큰 축 추가 — 통과율·지연이 같아도 토큰 ≥10% 절약이면 채택 근거. `runCycle`은 `runTournament`의 얇은 래퍼, `/cycle` 라우트가 `candidates[]`를 받는다. `cycleWallClockMin`을 30→90으로 올렸다 — 다수 후보 측정이 30분을 넘길 수 있어 좀비 잠금 회수가 정상 사이클을 죽이는 걸 방지.

**토너먼트 실모델 실측 (같은 날)** — CEO 역할문 후보 3개(도구 확인 의무화 / 도구 확인+답변 형식 엄격화 / 표현 다듬기 대조군)로 실측 완주: 18개 격리 팔 실행, 후보별 토큰 분리 계측(1915~1964), 원장 entries 기록, 실DB 무수정 확인. 승자 #1은 통과율 67%→100%였으나 지연 7s→9s(+29%)로 비열등 게이트(+10%)에 걸려 **discard** — 규칙대로 동작. 사유 표현은 "채택"이 아닌 "승자"로 수정해 최종 판정과 구분했다. 관찰된 긴장점: 통과율 +33%p가 지연 +2s에 뒤집힌 사례 — "정확도 최우선" 소유자 우선순위와 계약 비열등 규칙의 해석 여지가 있어 완화 여부는 사용자 판단에 남김.

**반복 표본 분산 실측 + benchReps 도입 (같은 날)** — 동일 설정(후보=기준선 역할문)으로 과제당 팔 4회씩 24회 격리 실행(`server/e2-variance-bench.ts`). 결과: G1(결정적 체크) 8/8·G7(lifecycle) 8/8 안정, **G6(eval_min) baseline 2/4·candidate 2/4 — 단일 표본이 동전 던지기 수준으로 요동**. 결론: eval_min 과제의 1회 판정은 노이즈를 실력으로 오판할 수 있다. 도입: `surfaces.json` `limits.benchReps`(기본 3) — eval_min 체크가 있는 과제만 과제당 reps회 격리 실행(결정적 과제는 1회 유지해 비용 절약), 팔별 브로커 예산도 실제 실행 수에 맞춰 스케일. holdout 재측정에도 같은 reps 적용. 회귀 322 pass / 0 fail.

**브라우저·MCP 도구 샌드박스 정책 (같은 날)** — 브로커 `/tool` 대행을 `web_search`에서 확장. worker 측은 `browserTool`·`mcpCall`에 e2 분기(`MYBOT_ENV=e2` + `E2_BROKER_URL`)를 두고, 부모 핸들러가 실제 도구를 대행한다. 허용 상한은 코드 고정: **읽기 전용 브라우저 5종**(`browser_open·read·scroll·wait·back`) + **과제가 `tools`로 명시 선언한 MCP 도구**(`서버__도구`). 변형·자격증명·임의실행 도구(`browser_click·type·eval·login·handoff·look`, `ego_run`, `bsk`, `computer_*`)는 과제 선언과 무관하게 핸들러가 `unsupported_tool`로 거부 — 부작용 호출은 측정이 아니라 운영 행동이므로. 브라우저 페이지는 팔별 키(`e2-<tag>-<runId>`)로 분리하고 팔 종료 시 `closePagesForPrefix`로 정리. 골든 과제는 `tools` 필드로 필요 도구를 선언하고 `sandboxToolOk`가 bench/holdout 진입을 걸러 미지원 선언 과제를 제외한다. **실모델 실측 완료**: G11(example.com 제목 확인) 양팔 통과 — 샌드박스 worker가 브로커 경유로 실제 headless Chromium을 열어 실제 제목을 읽고 답했고 팔별 `toolCalls` 계측이 기록됐다. 덤으로 G10의 잠복 결함(`tool_used` 타입에 `tool_prefix` 필드라 영구 실패)도 수정. 회귀: 전체 **315 pass / 0 fail**, tsc 0. 남은 한계: 로그인 필요 브라우저 과제(G10류)와 변형 도구 과제는 계속 env 제외, MCP는 서버 미구성이라 경로만 검증(실서버 대행은 미실측).

**릴리스 주기 기준 보강** — 소유자 지적: 릴리스가 너무 잦다. 원인은 메이저에 게이트가 없던 것 — 자기개선 작업은 `server/src/evolve.*`·`evolve/`를 건드려 `classifyTier`상 전부 메이저로 분류되고, 마이너만 묶음 규칙(3건+/72h)이 있어 커밋마다 즉시 적용 가능이었다. 기준 확정: **긴급패치** = `긴급`/`hotfix`/`fix!` 접두사, 즉시(탈출구) / **마이너** = 3건+ 또는 첫 커밋 72h (기존) / **메이저** = 마지막 커밋이 release에 오른 뒤 **12시간 정착** — 추가 커밋이 오면 타이머 리셋으로 연속 작업이 한 번의 적용으로 묶이고, 건수 대신 시간을 쓴 이유는 단건 대형 작업이 영원히 대기하지 않게 하려는 것. `evaluateRelease`가 `newestAgeMs`를 받고 `releaseStatus`·`cut-release`가 최신 커밋 시각을 계산해 전달. UI는 기존 reason 표시를 재사용한다.

### 1단계 작업 기록과 남은 인수 체크리스트

- [x] 필수 접속 경로 확정: `http://localhost:5274/`, `https://bot.myxcloud.co.kr/`.
- [x] 도메인의 로컬 Nginx → `127.0.0.1:5274` 연결 확인. Nginx 설정은 변경하지 않음.
- [x] 1단계만 분리한 코드 후보 준비: `codex/mybot-access-stage1`, 커밋 `4e355f2dd0a9af7cba5fd477b54c4a228edf1609`.
- [x] `MyBot-dev` 작업 트리에 1단계 소스 적용. 기존 `server/dev-s5-tick.ts`는 보존.
- [x] 인증 누락 차단, Host·Origin 제한, 로그인 화면, 헤더 기반 SSE·이미지 인증, 암호 파일 안전 검사 구현.
- [x] 접근 통제·SSE 테스트 9개 통과. 타입 검사·웹 빌드는 이전 작업에서 실행한 결과이며 이번 문서 갱신에서 재실행하지 않음.
- [ ] dev 접속 암호 준비 및 실제 로그인·실시간 화면·이미지 확인.
- [ ] dev 실행 버전과 검증된 코드·웹 빌드의 일치 확인.
- [ ] 서비스 전달용 릴리스 묶음·검증 영수증·복구 계획 준비.
- [x] 서비스 접근 통제 및 관리자 릴리스 조회 경로의 응답 확인(각각 미인증 401, 인증 200).
- [ ] 정식 release 경로의 수령·사용자 승인·적용·재시작 영수증 확보. 현재 코드 반영과 구분.
- [ ] 서비스에서 인증 전 차단 / 인증 후 정상 응답, localhost·외부 도메인 접속 확인.

개발 작업 트리에는 아직 미커밋 변경이 있다. 위 커밋은 최초 분리한 **코드 후보**이며 이후 서비스에 반영된 코드의 현재 버전과 같다고 가정하지 않는다. 본 점검에서는 암호 생성·재시작·웹 빌드 교체를 하지 않았다. 다른 작업에서 서비스 접근 통제·release 코드가 추가됐고 현재 API 응답을 위 재점검에 기록했다. 외부 기기 도메인 접속 인수 검증은 미완료다.

1단계의 최초 후보는 위 커밋을 참조하되, 다음 릴리스는 이미 서비스에 들어간 후속 수정을 대조한 뒤 확정한다. dev의 다른 미커밋 변경·기존 미추적 스크립트·2~5단계 초안은 자동 포함하지 않는다. 이전 문서 갱신의 코드 15개 일치 확인은 그 당시 결과이고, 이번에는 서비스 후속 변경 및 dev의 별도 모델 오류 처리 변경을 확인했다. 테스트는 재실행하지 않았다.

### 다음 작업: 기존 릴리스 경로를 기준으로 dev 검증 보강

서비스에 관리자용 `release.ts`와 조회 API가 추가되어, 이전의 “전체 릴리스 통로를 새로 만들어야 한다”는 판단은 갱신한다. `evolve.ts`의 보호 경로를 푸는 것은 여전히 금지하며, 신뢰 등급이 다른 두 경로를 유지한다.

1. [현재 작업 기록](tasks/todo.md)의 알림·승인 UX는 dev 소스에 통합했다. 실행 중인 dev·서비스 프로세스는 변경하지 않았으며, 이후 서비스 업그레이드 전에 실행 버전·접속·실제 채널 전송을 별도로 확인한다.
2. dev의 미커밋 변경을 보존하고 서비스 후속 커밋과 차이를 검토해 개발 기준을 맞춘다. 이 재점검에서는 병합·cherry-pick·브랜치 이동을 하지 않았다.
3. E1·E2-A·E2-B(브로커·골든 브리지·도구 대행·실모델 실측)는 dev에서 구현·회귀 검증을 마쳤다. E3 holdout 게이트·E4 팔별 예산·비용 기록·E7 서비스 영수증 검증(수령·적용 재검증+인증 발송)도 도입됐다. 다음은 대량 반복 실측과 자동 사이클 재개 검토다. 브라우저·MCP·승인 과제 정책과 keep→수령→적용 실측 왕복은 완료됐다. 업데이트의 효과 근거가 갖춰지기 전 빈도·예산을 늘리지 않는다.
4. 기존 release 경로에 승인 SHA·검증 영수증·적용 전 저널·재시작 후 건강 검사 등 R1~R3를 보강한다. 보호 코드와 실제 배포는 사람이 검토한 릴리스로만 승격한다.
5. dev 실제 실행·접속 검증과 버전 일치 확인을 마친 후보만 로컬 release 채널에 전달하고, 서비스 관리자가 그 후보를 확인해 적용한다. 현재 `canApply=true`만으로 이 완료 조건을 충족했다고 판단하지 않는다.

단계 번호는 계속 추적하되, 이미 추가된 기능을 중복 구현하지 않고 새 증거에 따라 남은 작업을 좁힌다.

개발 작업의 완료 기준(완료와 미완료 구분):

- [x] E1: 평가 호출 오류·깨진 JSON·누락 점수·취소·비유한 집계에서 keep을 차단하는 dev 회귀 검증 완료. 서비스 수신 API의 전체 검증은 E7에서 도입했다.
- [x] E7: 업데이트 패키지에 `evidence`(실험 id·팔별 격리 영수증·holdout·브로커 계측·`candidateKey`=ops 지문)를 결속하고, 서비스가 수령·적용 시 `verifyUpdatePackage`로 재검증한다. keep이 아닌 판정·불완전 벤치·영수증 해시 누락·실모델 미실행·ops-측정 불일치·holdout 누락은 400+rejected 기록. 구형 pending 패키지도 적용 시 재검증에 걸려 마감된다. keep 판정 시 `publishUpdate`가 서비스 자격 증명(`MYBOT_SERVICE_KEY`/키 파일/서비스 DB `access_code`)으로 발송하고 실패하면 `updates-outbox`에 보관. 검증 12개 테스트 통과. 남은 것: 실제 keep→발송→수령→적용 왕복 실측.
- [x] E2-A: 실제 dirty bytes의 SHA-256으로 앱 소스를 한 번 동결하고, 기준선·후보를 독립 Bun 프로세스와 별도 합성 DB/workspace에서 실행한다. 후보 전용 literal probe, 코드·하네스·정책·실행 파일·의존성 해시와 PID/종료 영수증을 fixture로 검증했다.
- [ ] E2-B: 격리 worker에 실제 골든/모델을 연결하되 자격 증명 broker, 외부 도구 허용 범위, 승인·취소 정책을 갖추고 운영 데이터·키를 복사하지 않는다. 현재 production runner 결과는 항상 승격 불가다.
  - 진행: 자격 증명 broker(`evolve/broker.ts`) — 부모 프로세스가 루프백 포트에 서버를 열고 샌드박스 정책이 그 포트 outbound만 허용(`remote tcp "localhost:PORT"`). worker는 `ctx.chat`으로 토큰 인증 호출만 가능하고 모델 허용목록·호출/토큰 상한을 브로커가 집행한다. 샌드박스 내 왕복·허용목록 차단·타 포트 차단을 fixture 테스트로 검증함.
  - 진행: **골든 브리지 완료** — worker가 브로커를 OpenAI 호환 프로바이더로 등록해 `runAgentDetached`로 실제 골든 과제를 실행한다. 모델→도구→모델 왕복·골든 체크·영수증 model 계측을 샌드박스 통합 테스트로 검증(279 pass). 토큰 `:arm` 태그로 팔별 비용 분리, `GET /usage`로 부모가 팔별 usage를 읽어 영수증에 기록. worker는 응답 출력 후 명시적 `process.exit`(파이프라인 타이머가 이벤트 루프를 잡아도 deadline kill 방지). 미지원 체크 유형·체크 0개는 미통과 fail-closed.
  - 진행: **도구 브로커 + 실모델 실측** — `/tool` 엔드포인트로 허용 도구(web_search)를 부모가 대행, 샌드박스에서도 검색 과제 측정 가능(G6 env 해제). `maxCallsPerTag`·`maxToolCallsPerTag`로 팔별 대칭 예산. dev 실키로 `zai/glm-5.3-flash` 실측 완주(팔별 2회·~1.8k 토큰, agent_list·봇 수 정합). 시드 봇의 운영 모델이 허용목록에 없으면 403 나던 버그를 실측에서 발견해 허용목록을 시드 모델 전체로 확장.
  - 진행: **대량·반복 실측 1회** — 4과제(도구·db_count·web_search·eval_min·memory_save)×3반복×양팔 24회 실모델 실행. 결정적 체크 18/18 안정(토큰 팔별 ~1.8k 대칭). eval_min에서 **toolLog 미전달 버그** 발견 — 평가자가 "도구 미사용"으로 오판해 정상 응답을 45~50점으로 깎던 것. 수정 후 같은 응답 100점. 평가 불능은 재시도 1회로 완화(24회 중 0건). 잔여 실패 1건은 봇이 도구 미호출인데 호출 주장+수치 모순을 평가자가 35점으로 정직하게 포착한 것 — 벤치의 허위 감지가 실제로 동작함을 실증. 남은 한계: openai 모델 봇(CEO·Eggbot·그룹웨어팀장)은 dev 키 부재로 실측 불가 — 표준 골든 과제(G1–G9)는 서비스 측 키가 있어야 실측 가능.
  - 남음: 로그인 필요·변형 브라우저 과제와 MCP 실서버 대행의 실측, 대량 반복 실측.
  - 남음: production 모드에서 실제 골든 업무를 파이프라인으로 실행하는 브리지, 외부 도구·승인 범위 정책, E4 실비용 집계 연결.
- [x] E3(부분): 같은 격리 조건에서 기준선과 후보를 비교하고, 주 세트 keep 시 holdout을 같은 방식으로 재측정해 하락하면 discard한다. 남은 것: 과제별 반복 표본(현재 과제당 1회/arm)으로 분산을 잡는 것.
- [ ] R2: 다음 서비스 릴리스를 허용하기 전에 병합·테스트·빌드·재시작 실패를 주입해 코드·웹 빌드의 복구 결과와 새 프로세스 health를 확인한다. 실패하면 복구 완료로 표시하지 않는다.

### 진행 기록 규칙

각 단계 갱신 시 날짜, 변경 범위, 후보 버전, 실행한 검증과 결과, 실행하지 않은 검증, dev 소스/실행 상태, 서비스 전달/적용/실행 상태, 남은 작업을 적는다. 비밀값·사용자 데이터는 기록하지 않는다. 실패·보류 사유는 지우지 않고 후속 해결 근거와 연결한다.

---

## 1. 초기 점검 기록 — 이후 진행 상태와 구분

아래의 운영 상태·57개 테스트·keep 기록은 초기 점검 당시 기록이다. 자체 개선의 안전성이나 이번 업그레이드 완료를 증명하지 않는다. 독립 계측·비용·안전 게이트의 보강은 위 3~4단계의 미완료 과제다.

| 영역 | 상태 | 근거 |
|---|---|---|
| 조직 설계 | 양호 | 봇 트리·역할문·모델 배정·루틴 무결성 위반 **0건** |
| 운영 안정성 | 회복됨 | **오늘(9/19) 실패 0건**. 마지막 실패는 9/18 15:12(토큰 만료) |
| 자기개선 | 가동 중 | dev에서 매일 03시 실행. 3사이클 중 **keep 1건**(통과율 0→30%, 지연 1000초→45초) |
| 테스트 | 57 pass / 0 fail | 8개 파일, tsc 오류 0 |
| 기술 부채 표식 | 없음 | TODO·FIXME **0건** |

**최근 7일 실패 177건의 내역** (모두 9/14~9/18, 오늘은 0건):

| 사유 | 건수 | 마지막 발생 | 조치 상태 |
|---|---|---|---|
| 429 한도 | 65 | 9/16 02:25 | 해결 — 실패 종류 무관 쿨다운 + 기본 모델 교체 |
| 사용자·시스템 중단 | 44 | 9/18 01:35 | 해당 없음(의도된 중단) |
| 401 크레딧 부족 | 32 | 9/18 01:27 | 해결 — 인증 실패도 폴백 |
| 타임아웃 | 25 | 9/18 00:58 | 부분 해결 — 브라우저 단계 예산 상향 |
| chatOnce 실패 | 7 | 9/16 08:54 | 미규명 (건수 적음) |
| 401 토큰 만료 | 3 | 9/18 15:12 | 해결 — 인증 실패 폴백 |

---

## 2. 개선 과제

### [높음] A-1. 점검의 실패율 창이 과거를 지나치게 반영한다

- **근거**: `org_audit`이 "위험: 최근 7일 실패율 15%(177/1220)"를 보고하지만, 그 177건은 전부 9/14~9/18이고 **오늘은 0건**이다. 이미 고친 문제가 일주일 내내 위험으로 남는다.
- **제안**: 실패율을 7일 단일 창이 아니라 **최근 24시간 / 7일 두 값으로 보고**하고, 24시간 값이 임계 이하면 심각도를 낮춘다. 추세(개선 중/악화 중)를 한 줄로 덧붙인다.
- **효과**: 매주 점검 보고서가 "이미 고친 문제"로 채워지지 않는다. 지금 조치가 필요한 것만 위험으로 남는다.
- **난이도**: 낮음 (`audit.ts` 한 규칙 수정 + 테스트)

### [높음] A-2. 자기개선 후보가 실재하지 않는 대상을 만든다

- **근거**: 9/19 03:44 사이클 판정 — `discard / preflight 실패: 대상 없음: skills.자기개선-실패점검과중단복구`. 존재하지 않는 스킬을 대상으로 후보를 생성해 사이클이 통째로 헛돌았다. 3사이클 중 1건이 이 사유다.
- **제안**: 후보 생성 단계에 **실재 대상 목록을 코드로 주입**한다(스킬명·봇명·루틴명을 DB에서 뽑아 프롬프트에 넣고, 목록 밖 대상은 preflight 이전에 거부). 지금은 preflight에서 걸러지지만, 애초에 만들지 않는 편이 낫다.
- **효과**: 하루 1회뿐인 사이클이 헛돌지 않는다. 개선 시도 실효율이 올라간다.
- **난이도**: 중간 (`evolve.ts` 후보 생성부)

### [중간] A-3. 실행 예산이 자기개선 표면에 없다

- **근거**: `tasks/self-improvement-contract.md` §2에 기록된 갭. `tool_rounds`·`tool_rounds_browser`·`run_deadline_sec`는 DB 설정값이 코드 기본값보다 우선하는데 어느 표면에도 없어, 루프가 예산을 스스로 조정하지 못한다. 실제로 "단계 상한으로 업무 미완료 114건"은 사람이 진단·수정했다.
- **제안**: `settings.budget` 표면을 **키 화이트리스트 + 상한**과 함께 추가한다(예: `tool_rounds` ≤ 30, `run_deadline_sec` ≤ 1200). 상한 없이 열면 루프가 완수율을 올리려 예산만 키우고, 비용은 판정기가 보는 지표가 아니라 폭증을 못 막는다.
- **효과**: 반복 발생하는 예산 문제를 루프가 스스로 고친다.
- **난이도**: 중간 (surfaces.json + `applyCandidate` 처리 + preflight 상한 검사)

### [중간] A-4. 안전장치 모듈에 테스트가 없다

**재점검 보정:** 초기 점검 뒤 서비스에는 `approvals.test.ts`와 `release.test.ts`가 추가됐다. 현재 dev에는 아직 없으므로 먼저 소스 차이를 검토해야 한다. “아무 테스트도 없음”이 아니라 **동시 승인·후보 격리·배포 실패·재시작·복구의 통합 검증 공백**으로 범위를 좁힌다. 이번에 테스트를 재실행하지 않았다.

- **근거**: 테스트 없는 대형 모듈 — `browser.ts` 842줄, `evolve.ts` 727줄, `adapters.ts` 453줄, **`approvals.ts` 360줄**, `db.ts` 335줄.
- 초기 점검 당시 `approvals.ts`는 자기개선 **보호 경로**인데 전용 테스트가 없었다. 현재는 위 재점검 보정처럼 서비스에 테스트가 추가됐으므로 미검증 시나리오를 중심으로 보강한다.
- **제안**: `approvals.ts`부터 테스트를 붙인다 — 승인 대상 판정(`approvalDecision`), 중복 요청 교체, 연속 실패 차단, 만료 처리. 그다음 `evolve.ts`의 preflight·판정 로직.
- **효과**: 보호 경로 변경 시 회귀를 코드가 잡는다.
- **난이도**: 중간

### [중간] A-5. 비활성 스킬이 방치돼 있다

- **근거**: `org_audit` 주의 항목 — `browser-skill`이 성공률 미달로 자동 비활성(15,559자). 켜지도 지우지도 않은 상태로 남아 있다.
- **제안**: 실패 이력을 확인해 ① 절차를 고쳐 재활성 ② 삭제 중 하나로 정리한다. bsk(사용자 Chrome 제어) 경로를 계속 쓸 것인지 결정이 선행돼야 한다.
- **효과**: 점검 보고서의 상시 주의 항목이 사라지고, 봇이 쓸 수 없는 스킬을 후보로 고려하지 않는다.
- **난이도**: 낮음 (판단 필요)

### [낮음] A-6. 자기개선 사이클이 하루 1회뿐이다

- **근거**: 역사적으로 `surfaces.json`의 `cyclesPerDay: 5`인데 `startEvolveLoop`는 설정 시각(기본 03시)에 **1회만** 실행했다. 현재 E2-A 소스에서는 tick이 모델 제안·계측 전에 보류되므로 자동 개선 시도는 E2-B까지 0이다.
- **제안**: 안전 보강과 A-2를 먼저 마친 뒤, 사이클 성공률·실제 비용을 측정해 1일 2~3회 확대 여부를 검토한다. 비용 영향은 현재 미측정이므로 제한적이라고 가정하지 않는다.
- **효과**: 개선 축적 속도가 올라간다.
- **난이도**: 낮음 (다만 A-2 선행 권장)

### [낮음] A-7. 승인 만료율 모니터링

- **근거**: 누적 만료 51건. `shell_run`을 승인 대상에서 뺀 뒤로는 새 만료가 생기지 않았지만, 만료율 규칙(`ops.expired_rate`)은 누적값을 보므로 한동안 주의로 뜰 수 있다.
- **제안**: 만료율도 A-1과 같은 방식으로 최근 구간 기준으로 본다.
- **난이도**: 낮음 (A-1과 함께 처리)

---

## 3. 이미 해결된 사항 (재발 감시 대상)

아래는 이번 주에 고친 것들이다. 같은 증상이 다시 보이면 원인을 새로 찾지 말고 이 목록을 먼저 확인한다.

| 증상 | 원인 | 조치 | 감시 지표 |
|---|---|---|---|
| CEO 역할문에 같은 문단 20회 누적 | 가드 키워드가 덧붙이는 문단에 없어 비멱등 | 런타임 조립으로 전환 | `org_audit`의 `role.repeat` |
| 업무가 승인 대기로 멈춤 | `ls` 같은 조회까지 승인 대상 | 초기에는 `shell_run` 전체 면제. 이후 서비스는 조회 명령만 자동 허용하도록 변경(`approvals.ts:43`), dev와 차이 있음 | `ops.stale_approval` 및 명령별 승인 정책 |
| "도구 단계 상한으로 미완료" | 브라우저 업무에 12라운드는 부족 | 32라운드 + 3/4 지점 예산 경고 | `ops.step_cap` |
| 죽은 모델 반복 호출(502 34회) | 429만 쿨다운 대상 | 실패 종류 무관 쿨다운 | 로그의 폴백 반복 |
| 메일 요약이 제목만 | 스킬이 브라우저 절차뿐 | IMAP 도구 + 목록에 본문 미리보기 | 브리핑 품질 |
| 설정 입력 한 글자마다 포커스 유실 | 하위 컴포넌트를 렌더 함수 안에서 정의 | 모듈 스코프로 이동 | — |
| 정기 스킬 4개가 같은 시각에 전부 실패 | 콘텐츠 필터 거부(400 코드 1301)가 폴백 대상이 아니라, 체인을 두고도 그 자리에서 죽음 | 폴백 대상에 추가하되 쿨다운은 걸지 않음(모델이 아니라 그 입력이 거부된 것) — `09ed896` + 회귀 테스트 4개 | 스킬 실패 사유의 `contentFilter` |

---

## 4. 기존 개선 백로그 순서 — 안전 보강 이후 재평가

문서 상단의 5단계 번호는 계속 추적한다. 현재 서비스에 접근 통제와 release 경로가 추가된 사실을 반영해 **dev 기준 정합성 → E1~E3 근거 검증 → 나머지 안전·배포·수명주기 보강**을 우선한다. 아래 A 계열은 기존 백로그이며 이 안전 조건보다 먼저 빈도·예산 확대를 진행하지 않는다.

1. **A-1**(점검 창) — 가장 싸고, 이후 모든 주간 점검의 신호 대 잡음비를 올린다
2. **A-2**(후보 대상 검증) — 자기개선이 실제로 굴러가기 위한 전제
3. **A-5**(비활성 스킬 정리) — 판단만 하면 되는 건
4. **A-4**(`approvals.ts` 테스트) — 안전장치 회귀 방지
5. **A-3**(예산 표면) — A-2 안정 후
6. **A-6**(사이클 빈도) — 마지막

각 항목은 착수 전에 현재 수치를 다시 측정하고, 조치 후 같은 수치로 효과를 확인한다. 측정 없이 "개선했다"고 적지 않는다.

### 상시 과제 (끝나지 않는 지표)

- **브라우저 계열 스킬 신뢰성** — `tasks/browser-reliability.md`. 사용자가 상시 개선 사항으로 지정(2026-09-20): 그룹웨어·브라우저 스킬의 성공률을 계속 올리고, 더 나은 경로가 있으면 옮긴다. 새 실패가 쌓일 때마다 원인을 인프라/절차로 나눈다. 브라우저 세션은 백그라운드(`--no-focus`)로 시작한다.

  지금까지 기록된 실패는 전부 LLM 호출 계층 원인이고 절차 실패는 0건이다 — 잔액 소진 4건, 타임아웃 2건, 콘텐츠 필터 거부 12건. 앞의 둘은 조치했고, 타임아웃은 `AbortSignal.timeout()`의 기본 문구라 어느 지점인지 알 수 없어 **한도를 늘리기 전에 계측이 먼저**다.

  **재측정을 생략하지 않는다.** 2026-09-21에 이 규칙이 값을 했다 — 하루 전 수치를 믿고 "조치 완료"로 넘겼다면 그 사이 늘어난 12건이 전부 새 원인이라는 걸 놓쳤을 것이다. 여러 세션이 같은 저장소에서 작업하므로 **착수 전 `git log`로 다른 세션의 최근 커밋도 함께 확인한다**(같은 건을 두 번 고칠 뻔한 실측이 있다).

---

## 5. 점검 재현 방법

```bash
# 조직 설계 점검 (규칙 기반 — 실행 모델과 무관하게 동일한 결과)
bun -e 'const {auditOrg,formatAudit}=await import("./server/src/audit.ts");console.log(formatAudit(auditOrg()))'

# 실패 원인 분해 (최근 7일)
sqlite3 "file:server/data/mybot.db?mode=ro" "SELECT status, COUNT(*) FROM agent_runs WHERE created_at > (strftime('%s','now')-7*86400)*1000 GROUP BY status;"

# 자기개선 이력 (dev 인스턴스에서 실행)
sqlite3 "file:server/data/mybot.db?mode=ro" "SELECT datetime(created_at/1000,'unixepoch','localtime'), surface, target, verdict, reason FROM experiments ORDER BY created_at DESC;"

# 전체 테스트
bun test server/src
```

봇에게 시킬 때는 `org_audit` 도구를 쓰게 한다 — 주 1회 점검 루틴이 이미 그렇게 되어 있다.

### 2026-09-20 (후속): Aside 브라우저 분석 — 도입 후보

Aside(YC F25, AI 브라우저)의 공개 기술 블로그·문서를 분석해 MyBot에 적용 가능한 후보를 정리했다(벤치 수치는 벤더 자체 실행이므로 참고용). 자세한 매핑은 대화 기록 참조.

- **스냅샷 정제(후보 A) → 채택(9ac9472)** — 이름없는 비폼 요소 제거·포커스/펼침/선택 상태 비트·연속 동일 요소 @5,6,7 압축·본문 중복 줄 제거. 실측: 크기는 큰 페이지에서 거의 동일(기존 스냅샷이 이미 타이트 — Aside의 70% 주장은 다른 기준선 대비), 구성 개선이 본질. G11 실모델 양팔 통과·315 테스트 통과. 원래 후보 설명:  Aside는 자체 a11y 트리로 중간 요소를 휴리스틱 제거하고 포커스·iframe·클릭가능 상태를 추가해 경쟁 구현 대비 ~70% 토큰 절감, 핵심 정보를 전면 배치. MyBot의 `snapshot()`도 @번호+텍스트 잘림이 있지만 중간 요소 정제·중요도 정렬은 없다. 도입 시 골든 브라우저 과제(G11류)로 팔별 토큰·통과율 실측 비교 가능.
- **REPL 중심 브라우저 조작(후보 B) → 실측 후 불채택** — dev 실파이프라인 A/B(G11~G13): eval-우선 역할문은 G12에서 탐색 없이 지식 답변(도구 0회)으로 실패, ref-우선은 현행과 동일 통과. 개선 증거 없음. 기존 "@번호 우선·eval은 예비" 지침 유지가 정답이었다.
- **작업별 권한 모드(후보 C) → 채택(9d2ebd9)** — command_jobs.task_mode(readonly|guard): readonly는 읽기 도구만, guard는 읽기 외 전부 승인 큐. rootJobId로 위임 하위 봇에도 상속. Composer에 권한 선택 추가.
- **자격증명 사용 감사(후보 D) → 채택(9d2ebd9)** — credential_uses 테이블: autofill 성공/실패/인계·계정요청 승인/거절을 봇·실행·결과와 함께 기록, GET /api/sites/uses. 비밀번호 미포함.
- **루틴 변형(후보 E) → 채택** — heartbeat는 이미 동일(MyBot 루틴이 봇 세션 대화에 붙어 맥락 상속). 실제 갭 두 개를 채움: 같은 봇이 실행 중이면 루틴 발화 건너뜀(overlap skip), 반복 실행 이력 스캔→루틴 제안(GET /api/routines/suggestions + 설정 UI).
- **실행 중 스티어(후보 F) → 채택** — run_steers 테이블 + POST /api/chat/steer + runAgent 라운드마다 미소비 지시 주입. MyBot의 대기열이 Aside의 Queue에 해당, '지금 지시' 버튼이 Steer에 해당.
- **비동기 브라우저 이벤트 푸시(후보 G) → 채택(a1e8835)** — Aside의 agent signals. 팝업 열림·다운로드 시작·탭 닫힘·대화상자·페이지 이동을 키별 큐에 모아 다음 browser_* 결과 끝에 [브라우저 이벤트]로 붙임. 브로커 경유 E2도 부모 측 래퍼가 동일 처리. 실동작 확인(네비게이션 이벤트 출력·이벤트 없으면 섹션 없음)·322 테스트 통과.
- **Ultrabrowse식 딥리서치 → 이미 동등** — deepsearch.ts가 이미 쿼리 분해→다라운드 검색→출처 본문 정독→[N] 인용 종합(외부 지시 무시 경고 포함)을 수행. 별도 도입 불필요.
- **Asidewright급 자체 브라우저 → 보류** — Chromium 패치(포커스·봇탐지 회피)는 stock Playwright로 불가. 필요 시 별도 빌드 파이프라인이 필요한 대형 과제.
- **주의 원칙** — Aside는 "AI가 프롬프트를 쓰게 하지 않는다"(자동 생성은 slop을 쌓는다)를 원칙으로 둔다. MyBot의 자기개선은 역할문을 후보로 쓰므로 이 지적과 정면 충돌하지만, 토너먼트 측정 게이트가 slop을 걸러내는 구조로 답한다. 다만 탐색 지시에 "최소 침습 편집"을 명시할 가치 있음.
- **이미 동일한 것** — 고정 뷰포트(1440x900), 백그라운드 탭(--no-focus), 스텔스 init 스크립트, 승인 게이트(결제·발신 등 위험 액션 사용자 승인), 시크릿 비노출 자동입력.
